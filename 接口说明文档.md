# 考勤服务器接口说明文档

## 概述

本项目是一个基于TCP协议的考勤服务器系统，使用JSON格式的消息进行通信。系统支持两种客户端角色：
- **设备端 (device)**: 考勤设备，用于上报考勤记录、同步数据等
- **管理端 (admin)**: 管理后台，用于人员管理、考勤记录查询、设备管理等

## 协议格式

### 消息封装 (Envelope)

所有消息采用JSON格式，基本结构如下：

```json
{
  "type": "消息类型",
  "role": "角色(device/admin/server)",
  "from": "发送方标识",
  "to": "接收方标识",
  "msgId": "消息唯一ID",
  "inReplyTo": "回复的消息ID(响应消息)",
  "ts": 时间戳(毫秒),
  "code": 状态码,
  "msg": "状态消息",
  "data": { 业务数据 }
}
```

### 状态码定义

| 状态码 | 常量名 | 说明 |
|--------|--------|------|
| 0 | - | 成功 |
| 1001 | kCodeParseError | JSON解析错误 |
| 1002 | kCodePayloadTooLarge | 负载过大 |
| 2001 | kCodeNotAuthenticated | 未认证 |
| 2002 | kCodeAuthFailed | 认证失败 |
| 2003 | kCodeDuplicateSession | 重复会话 |
| 3001 | kCodeForbidden | 权限不足 |
| 4000 | kCodeBusinessValidation | 业务验证失败 |
| 4001 | kCodeEmployeeNotFound | 员工不存在 |
| 5001 | kCodeDeviceOffline | 设备离线 |
| 5002 | kCodeForwardTimeout | 转发超时 |
| 6001 | kCodeDuplicateKey | 重复键 |
| 6002 | kCodeDbError | 数据库错误 |

---

## 一、认证接口

### 1.1 管理端认证 (auth)

**请求类型**: `auth`

**角色要求**: admin

**权限要求**: 无

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | string | 是 | 用户名(员工ID) |
| password | string | 是 | 密码 |

**请求示例**:
```json
{
  "type": "auth",
  "role": "admin",
  "msgId": "msg-001",
  "data": {
    "username": "EMP001",
    "password": "password123"
  }
}
```

**响应类型**: `auth_response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| sessionToken | string | 会话令牌 |
| heartbeatSec | int | 心跳间隔(秒) |
| roles | array | 用户角色列表 |
| permissions | array | 用户权限列表 |

**响应示例**:
```json
{
  "type": "auth_response",
  "code": 0,
  "msg": "ok",
  "inReplyTo": "msg-001",
  "data": {
    "sessionToken": "admin-db-EMP001",
    "heartbeatSec": 30,
    "roles": ["admin", "super_admin"],
    "permissions": ["person.create", "person.read", ...]
  }
}
```

---

### 1.2 设备端认证 (auth)

**请求类型**: `auth`

**角色要求**: device

**权限要求**: 无

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deviceId | string | 是 | 设备ID |
| deviceKey | string | 是 | 设备密钥 |

**请求示例**:
```json
{
  "type": "auth",
  "role": "device",
  "msgId": "msg-001",
  "data": {
    "deviceId": "DEVICE001",
    "deviceKey": "device-key-123"
  }
}
```

**响应类型**: `auth_response`

**响应示例**:
```json
{
  "type": "auth_response",
  "code": 0,
  "msg": "ok",
  "inReplyTo": "msg-001",
  "data": {
    "sessionToken": "device-session-DEVICE001",
    "heartbeatSec": 30
  }
}
```

---

### 1.3 心跳 (heartbeat)

**请求类型**: `heartbeat`

**角色要求**: 已认证的任意角色

**权限要求**: 无

**请求参数**: 无

**响应类型**: `heartbeat_response`

---

## 二、人员管理接口

### 2.1 创建人员 (person.create)

**请求类型**: `person.create`

**角色要求**: admin

**权限要求**: `person.create`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 姓名 |
| employeeId | string | 是 | 员工ID |
| department | string | 是 | 部门 |
| position | string | 是 | 职位 |

**请求示例**:
```json
{
  "type": "person.create",
  "role": "admin",
  "msgId": "msg-002",
  "data": {
    "name": "张三",
    "employeeId": "EMP001",
    "department": "技术部",
    "position": "工程师"
  }
}
```

**响应类型**: `person.create.response`

---

### 2.2 查询人员 (person.query)

**请求类型**: `person.query`

**角色要求**: admin

**权限要求**: `person.read`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 否 | 姓名(模糊匹配) |
| employeeId | string | 否 | 员工ID |
| department | string | 否 | 部门 |
| position | string | 否 | 职位 |
| createdAt | string | 否 | 创建时间 |
| updatedAt | string | 否 | 更新时间 |

**响应类型**: `person.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| records | array | 人员记录列表 |

**响应示例**:
```json
{
  "type": "person.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "张三",
        "employeeId": "EMP001",
        "department": "技术部",
        "position": "工程师",
        "createdAt": "2024-01-01 10:00:00",
        "updatedAt": "2024-01-01 10:00:00"
      }
    ]
  }
}
```

---

### 2.3 更新人员 (person.update)

**请求类型**: `person.update`

**角色要求**: admin

**权限要求**: `person.update`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID(定位条件) |
| name | string | 否 | 新姓名 |
| department | string | 否 | 新部门 |
| position | string | 否 | 新职位 |

> 注：至少需要一个更新字段

**响应类型**: `person.update.response`

---

### 2.4 删除人员 (person.delete)

**请求类型**: `person.delete`

**角色要求**: admin

**权限要求**: `person.delete`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |

**响应类型**: `person.delete.response`

---

## 三、考勤记录管理接口

### 3.1 创建考勤记录 (attendance.create)

**请求类型**: `attendance.create`

**角色要求**: admin

**权限要求**: `attendance.create`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |
| checkTime | string | 是 | 打卡时间 |
| status | string | 是 | 考勤状态 |
| deviceId | string | 否 | 设备ID |

**响应类型**: `attendance.create.response`

---

### 3.2 查询考勤记录 (attendance.query)

**请求类型**: `attendance.query`

**角色要求**: admin

**权限要求**: `attendance.read`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | int | 否 | 记录ID |
| employeeId | string | 否 | 员工ID |
| checkTime | string | 否 | 打卡时间 |
| deviceId | string | 否 | 设备ID |
| status | string | 否 | 考勤状态 |
| receivedTime | string | 否 | 接收时间 |

**响应类型**: `attendance.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| records | array | 考勤记录列表 |

**响应示例**:
```json
{
  "type": "attendance.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "employeeId": "EMP001",
        "checkTime": "2024-01-01 09:00:00",
        "deviceId": "DEVICE001",
        "status": "check_in",
        "receivedTime": "2024-01-01 09:00:05"
      }
    ]
  }
}
```

---

### 3.3 更新考勤记录 (attendance.update)

**请求类型**: `attendance.update`

**角色要求**: admin

**权限要求**: `attendance.update`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| locate | object | 是 | 定位条件对象 |
| locate.id | int | 否 | 记录ID |
| locate.employeeId | string | 否 | 员工ID |
| locate.checkTime | string | 否 | 打卡时间 |
| locate.deviceId | string | 否 | 设备ID |
| locate.status | string | 否 | 考勤状态 |
| locate.receivedTime | string | 否 | 接收时间 |
| updates | object | 是 | 更新内容对象 |
| updates.checkTime | string | 否 | 新打卡时间 |
| updates.deviceId | string | 否 | 新设备ID |
| updates.status | string | 否 | 新考勤状态 |
| updates.receivedTime | string | 否 | 新接收时间 |

> 注：locate必须至少有一个条件，updates必须至少有一个字段

**响应类型**: `attendance.update.response`

---

### 3.4 删除考勤记录 (attendance.delete)

**请求类型**: `attendance.delete`

**角色要求**: admin

**权限要求**: `attendance.delete`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | int | 否 | 记录ID |
| employeeId | string | 否 | 员工ID |
| checkTime | string | 否 | 打卡时间 |
| deviceId | string | 否 | 设备ID |
| status | string | 否 | 考勤状态 |
| receivedTime | string | 否 | 接收时间 |

> 注：必须至少提供一个条件，防止全表删除

**响应类型**: `attendance.delete.response`

---

### 3.5 查询考勤归档记录 (attendance.archive.query)

**请求类型**: `attendance.archive.query`

**角色要求**: admin

**权限要求**: `attendance.archive.read`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | int | 否 | 记录ID |
| employeeId | string | 否 | 员工ID |
| name | string | 否 | 姓名 |
| department | string | 否 | 部门 |
| position | string | 否 | 职位 |
| checkTime | string | 否 | 打卡时间 |
| deviceId | string | 否 | 设备ID |
| status | string | 否 | 考勤状态 |
| receivedTime | string | 否 | 接收时间 |
| archivedAt | string | 否 | 归档时间 |
| archiveReason | string | 否 | 归档原因 |

**响应类型**: `attendance.archive.query.response`

---

### 3.6 删除考勤归档记录 (attendance.archive.delete)

**请求类型**: `attendance.archive.delete`

**角色要求**: admin

**权限要求**: `attendance.archive.delete`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |

**响应类型**: `attendance.archive.delete.response`

---

## 四、设备管理接口

### 4.1 创建设备 (device.create)

**请求类型**: `device.create`

**角色要求**: admin

**权限要求**: `device.create`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deviceId | string | 是 | 设备ID |
| deviceName | string | 是 | 设备名称 |
| ipAddress | string | 否 | IP地址 |
| lastOnline | string | 否 | 最后在线时间 |
| status | string | 否 | 状态 |

**响应类型**: `device.create.response`

---

### 4.2 查询设备 (device.query)

**请求类型**: `device.query`

**角色要求**: admin

**权限要求**: `device.read`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deviceId | string | 否 | 设备ID |
| deviceName | string | 否 | 设备名称 |
| ipAddress | string | 否 | IP地址 |
| lastOnline | string | 否 | 最后在线时间 |

**响应类型**: `device.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| records | array | 设备记录列表 |

**响应示例**:
```json
{
  "type": "device.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "deviceId": "DEVICE001",
        "deviceName": "一楼大门考勤机",
        "ipAddress": "192.168.1.100",
        "lastOnline": "2024-01-01 10:00:00",
        "status": "online"
      }
    ]
  }
}
```

---

### 4.3 更新设备 (device.update)

**请求类型**: `device.update`

**角色要求**: admin

**权限要求**: `device.update`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deviceId | string | 是 | 设备ID(定位条件) |
| deviceName | string | 否 | 新设备名称 |
| ipAddress | string | 否 | 新IP地址 |
| lastOnline | string | 否 | 新最后在线时间 |
| status | string | 否 | 新状态 |

> 注：至少需要一个更新字段

**响应类型**: `device.update.response`

---

### 4.4 删除设备 (device.delete)

**请求类型**: `device.delete`

**角色要求**: admin

**权限要求**: `device.delete`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deviceId | string | 是 | 设备ID |

**响应类型**: `device.delete.response`

---

## 五、用户账号管理接口

### 5.1 创建用户 (user.create)

**请求类型**: `user.create`

**角色要求**: admin

**权限要求**: `user.create`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |
| password | string | 是 | 密码(需满足强度要求) |
| name | string | 否 | 用户名称 |

> 密码强度要求：至少8个字符，包含大小写字母和数字

**响应类型**: `user.create.response`

---

### 5.2 查询用户 (user.query)

**请求类型**: `user.query`

**角色要求**: admin

**权限要求**: `user.read`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 否 | 员工ID |
| createdAt | string | 否 | 创建时间 |

**响应类型**: `user.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| records | array | 用户记录列表 |

**响应示例**:
```json
{
  "type": "user.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "employeeId": "EMP001",
        "name": "张三",
        "createdAt": "2024-01-01 10:00:00",
        "lastLoginTime": "2024-01-02 09:00:00"
      }
    ]
  }
}
```

---

### 5.3 更新用户 (user.update)

**请求类型**: `user.update`

**角色要求**: admin

**权限要求**: `user.update`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID(定位条件) |
| name | string | 否 | 新用户名称 |
| password | string | 否 | 新密码(需满足强度要求) |

> 注：至少需要一个更新字段

**响应类型**: `user.update.response`

---

### 5.4 删除用户 (user.delete)

**请求类型**: `user.delete`

**角色要求**: admin

**权限要求**: `user.delete`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |

> 注：不能删除最后一个super_admin用户

**响应类型**: `user.delete.response`

---

## 六、人脸数据管理接口

### 6.1 查询人脸数据 (face.query)

**请求类型**: `face.query`

**角色要求**: admin

**权限要求**: `face.read`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |

**响应类型**: `face.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| found | bool | 是否找到人脸数据 |
| employeeId | string | 员工ID |
| featureSize | int | 特征数据大小 |
| createdAt | string | 创建时间 |
| updatedAt | string | 更新时间 |

**响应示例**:
```json
{
  "type": "face.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "found": true,
    "employeeId": "EMP001",
    "featureSize": 2048,
    "createdAt": "2024-01-01 10:00:00",
    "updatedAt": "2024-01-01 10:00:00"
  }
}
```

---

### 6.2 删除人脸数据 (face.delete)

**请求类型**: `face.delete`

**角色要求**: admin

**权限要求**: `face.delete`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |

**响应类型**: `face.delete.response`

---

### 6.3 人脸注册 (face.register)

**请求类型**: `face.register`

**角色要求**: admin

**权限要求**: `face.register`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employeeId | string | 是 | 员工ID |
| photoBase64 | string | 是 | 人脸照片的 Base64 编码（支持 JPEG/PNG/BMP） |
| overwrite | boolean | 否 | 是否覆盖已存在的人脸数据，默认 false |

> 本接口为单条 JSON 消息，服务端自动完成人脸检测与特征提取。已替代旧有的 `face.register.header` + 二进制帧两步式接口。

**响应类型**: `face.register.response`

**成功响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| employeeId | string | 员工ID |
| featureSize | int | 提取到的特征向量长度（字节） |
| faceRect | object | 人脸位置（left/top/right/bottom 像素坐标） |
| isNew | boolean | 是否为新注册（true=首次，false=覆盖更新） |

**错误码**:

| 错误码 | 说明 |
|--------|------|
| 4000 | 缺少必填字段或 Base64 无效 |
| 4001 | 未检测到人脸 |
| 4002 | 检测到多张人脸 |
| 4003 | 人脸尺寸不足 |
| 4004 | 特征提取失败 |
| 4005 | 图片格式错误 |
| 4006 | 员工不存在 |
| 4007 | 人脸数据已存在且未设置覆盖 |

---

## 七、角色管理接口 (RBAC)

### 7.1 创建角色 (role.create)

**请求类型**: `role.create`

**角色要求**: admin

**权限要求**: `super_admin` 角色

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleKey | string | 是 | 角色键 |
| roleName | string | 是 | 角色名称 |
| description | string | 否 | 角色描述 |

**响应类型**: `role.create.response`

---

### 7.2 查询角色 (role.query)

**请求类型**: `role.query`

**角色要求**: admin

**权限要求**: `user.read`

**请求参数**: 无

**响应类型**: `role.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| records | array | 角色记录列表 |

**响应示例**:
```json
{
  "type": "role.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "roleKey": "admin",
        "roleName": "管理员",
        "description": "系统管理员",
        "isSystem": true,
        "createdAt": "2024-01-01 00:00:00",
        "updatedAt": "2024-01-01 00:00:00"
      }
    ]
  }
}
```

---

### 7.3 更新角色 (role.update)

**请求类型**: `role.update`

**角色要求**: admin

**权限要求**: `super_admin` 角色

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleKey | string | 是 | 角色键(定位条件) |
| roleName | string | 否 | 新角色名称 |
| description | string | 否 | 新角色描述 |
| permissions | array | 否 | 权限键列表 |

**响应类型**: `role.update.response`

---

### 7.4 删除角色 (role.delete)

**请求类型**: `role.delete`

**角色要求**: admin

**权限要求**: `super_admin` 角色

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| roleKey | string | 是 | 角色键 |

**响应类型**: `role.delete.response`

---

## 八、用户角色管理接口

### 8.1 分配用户角色 (user.role.assign)

**请求类型**: `user.role.assign`

**角色要求**: admin

**权限要求**: `super_admin` 角色

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | int | 是 | 用户ID |
| roleKey | string | 是 | 角色键 |

**响应类型**: `user.role.assign.response`

---

### 8.2 撤销用户角色 (user.role.revoke)

**请求类型**: `user.role.revoke`

**角色要求**: admin

**权限要求**: `super_admin` 角色

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | int | 是 | 用户ID |
| roleKey | string | 是 | 角色键 |

> 注：不能撤销自己的super_admin角色；至少保留一个super_admin用户

**响应类型**: `user.role.revoke.response`

---

### 8.3 查询用户角色 (user.role.query)

**请求类型**: `user.role.query`

**角色要求**: admin

**权限要求**: `user.read`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| userId | int | 是 | 用户ID |

**响应类型**: `user.role.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| roles | array | 角色键列表 |

---

## 九、权限查询接口

### 9.1 查询所有权限 (permission.query)

**请求类型**: `permission.query`

**角色要求**: admin

**权限要求**: `user.read`

**请求参数**: 无

**响应类型**: `permission.query.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| records | array | 权限记录列表 |

**响应示例**:
```json
{
  "type": "permission.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "permKey": "person.create",
        "permName": "创建人员",
        "resource": "person",
        "description": "创建人员信息"
      }
    ]
  }
}
```

---

### 9.2 查询当前用户权限 (permission.self)

**请求类型**: `permission.self`

**角色要求**: admin

**权限要求**: 无

**请求参数**: 无

**响应类型**: `permission.self.response`

**响应参数**:

| 参数名 | 类型 | 说明 |
|--------|------|------|
| permissions | array | 当前用户的权限键列表 |

---

## 十、设备端专用接口

### 10.1 上报考勤记录 (attendance.report)

**请求类型**: `attendance.report`

**角色要求**: device

**权限要求**: 无

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| employee_id / employeeId | string | 是 | 员工ID |
| check_time / checkTime | string | 是 | 打卡时间 |
| status / checkStatus | string | 是 | 考勤状态 |
| deviceId | string | 否 | 设备ID |
| awaitPhoto | bool | 否 | 是否等待照片 |

**响应类型**: `attendance.report.response`

> 注：若awaitPhoto为true，设备需在响应后发送`attendance.photo.header`消息和二进制照片数据

---

### 10.2 上报考勤照片头 (attendance.photo.header)

**请求类型**: `attendance.photo.header`

**角色要求**: device

**权限要求**: 无

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| payloadLength | int | 是 | 照片二进制数据长度 |

> 注：此消息后需紧跟二进制照片数据

---

### 10.3 上报设备状态 (device.status.report)

**请求类型**: `device.status.report`

**角色要求**: device

**权限要求**: 无

**请求参数**: 自定义状态数据

**响应类型**: `device.status.report.response`

---

### 10.4 数据同步请求 (sync.request)

**请求类型**: `sync.request`

**角色要求**: device

**权限要求**: 无

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| deviceId | string | 否 | 设备ID |

**响应流程**:
1. 服务器发送多条 `person.sync` 消息（人员数据分页）
2. 服务器发送 `face.sync.begin` 消息
3. 服务器发送多条 `face.sync.item.header` + 二进制特征数据
4. 服务器发送 `face.sync.end` 消息

---

### 10.5 同步确认 (sync.ack)

**请求类型**: `sync.ack`

**角色要求**: device

**权限要求**: 无

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| status | string | 是 | 同步状态("ok"或其他) |
| message | string | 否 | 消息 |

---

### 10.6 设备命令确认 (device.command.ack)

**请求类型**: `device.command.ack`

**角色要求**: device

**权限要求**: 无

**说明**: 设备收到`device.command`后发送此确认消息

---

## 十一、事件订阅接口

### 11.1 订阅事件 (subscribe)

**请求类型**: `subscribe`

**角色要求**: admin

**权限要求**: `event.subscribe`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| topics | array | 是 | 主题列表(最多32个) |

**请求示例**:
```json
{
  "type": "subscribe",
  "role": "admin",
  "msgId": "msg-010",
  "data": {
    "topics": ["attendance", "device"]
  }
}
```

**响应类型**: `subscribe.response`

---

### 11.2 取消订阅 (unsubscribe)

**请求类型**: `unsubscribe`

**角色要求**: admin

**权限要求**: `event.subscribe`

**请求参数**: 无

**响应类型**: `unsubscribe.response`

---

### 11.3 考勤推送 (attendance.push)

**消息类型**: `attendance.push`

**说明**: 服务器主动推送给订阅了"attendance"主题的管理端

**推送数据**:
```json
{
  "type": "attendance.push",
  "role": "server",
  "from": "server",
  "msgId": "push-xxx",
  "ts": 1704067200000,
  "data": {
    "employee_id": "EMP001",
    "check_time": "2024-01-01 09:00:00",
    "status": "check_in",
    "device_id": "DEVICE001"
  }
}
```

---

## 十二、设备命令转发接口

### 12.1 发送设备命令 (device.command)

**请求类型**: `device.command`

**角色要求**: admin

**权限要求**: `device.command`

**请求参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| to | string | 是 | 目标设备ID |
| data | object | 是 | 命令数据 |

**请求示例**:
```json
{
  "type": "device.command",
  "role": "admin",
  "msgId": "msg-020",
  "to": "DEVICE001",
  "data": {
    "command": "restart",
    "params": {}
  }
}
```

**响应**: 设备返回的 `device.command.ack` 将转发给管理端

---

## 权限列表

| 权限键 | 说明 |
|--------|------|
| person.create | 创建人员 |
| person.read | 查询人员 |
| person.update | 更新人员 |
| person.delete | 删除人员 |
| attendance.create | 创建考勤记录 |
| attendance.read | 查询考勤记录 |
| attendance.update | 更新考勤记录 |
| attendance.delete | 删除考勤记录 |
| attendance.archive.read | 查询考勤归档 |
| attendance.archive.delete | 删除考勤归档 |
| device.create | 创建设备 |
| device.read | 查询设备 |
| device.update | 更新设备 |
| device.delete | 删除设备 |
| device.command | 发送设备命令 |
| user.create | 创建用户 |
| user.read | 查询用户 |
| user.update | 更新用户 |
| user.delete | 删除用户 |
| face.register | 人脸注册 |
| face.read | 查询人脸数据 |
| face.delete | 删除人脸数据 |
| event.subscribe | 事件订阅 |

---

## 系统角色

| 角色键 | 说明 |
|--------|------|
| device | 设备端 |
| admin | 管理端 |
| server | 服务器 |
| super_admin | 超级管理员(拥有所有权限，可管理角色) |

---

## 附录：错误响应格式

所有接口在发生错误时返回统一格式：

```json
{
  "type": "error",
  "code": 3001,
  "msg": "forbidden",
  "inReplyTo": "原始消息ID",
  "to": "发送方标识"
}
```

---

*文档生成时间: 2026-05-14*
