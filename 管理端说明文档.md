# AttendanceAdmin 考勤管理端详细说明文档

## 1. 项目概述

### 1.1 基本信息

| 项目属性 | 说明 |
|---------|------|
| **项目名称** | AttendanceAdmin（考勤管理端） |
| **项目类型** | Qt/QML 桌面应用程序 |
| **构建系统** | CMake |
| **Qt 版本** | Qt 6.8+ |
| **编程语言** | C++17 / QML |
| **目标平台** | Windows / macOS |

### 1.2 主要依赖库

- **Qt6::Quick** - QML/Qt Quick UI 框架
- **Qt6::Core** - Qt 核心库
- **Qt6::Gui** - GUI 组件
- **Qt6::Network** - 网络通信（TCP）
- **Qt6::Widgets** - Qt Widgets 组件
- **Qt6::Charts** - 图表组件

### 1.3 项目定位

AttendanceAdmin 是考勤管理系统的管理端应用程序，采用客户端-服务器架构，通过 TCP 长连接与后端服务器通信，提供人员管理、设备管理、考勤记录、人脸库管理、用户权限管理等完整的企业考勤管理功能。

---

## 2. 目录结构

```
E:\project\AttendanceServer\
├── CMakeLists.txt                    # CMake 主配置文件
├── CMakePresets.json                 # CMake 预设配置
├── CMakeUserPresets.json             # 用户 CMake 预设
├── main.cpp                          # 应用程序入口点
├── .gitignore                        # Git 忽略文件配置
├── ui/                               # QML 用户界面目录
│   ├── Main.qml                      # 主窗口
│   ├── LoginPage.qml                 # 登录页面
│   ├── theme/
│   │   └── Theme.qml                 # 全局主题定义
│   ├── models/
│   │   ├── Logger.qml                # 日志管理模型
│   │   ├── History.qml               # 调用历史模型
│   │   ├── Presets.qml               # 预设值模型
│   │   ├── ErrorCatalog.qml          # 错误码目录
│   │   ├── PermissionCatalog.qml     # 权限目录
│   │   └── PushFeed.qml              # 推送消息模型
│   ├── components/
│   │   ├── Card.qml                  # 卡片容器组件
│   │   ├── BusyOverlay.qml           # 加载遮罩组件
│   │   ├── SideBar.qml               # 侧边栏导航组件
│   │   ├── StatusBar.qml             # 状态栏组件
│   │   ├── BadgeStatus.qml           # 状态徽章组件
│   │   ├── LabeledField.qml          # 带标签输入字段组件
│   │   ├── ToolBarRow.qml            # 工具栏行组件
│   │   ├── JsonEditor.qml            # JSON 编辑器组件
│   │   ├── JsonViewer.qml            # JSON 查看器组件
│   │   ├── DataTable.qml             # 数据表格组件
│   │   ├── LogPanel.qml              # 日志面板组件
│   │   ├── ConfirmDialog.qml         # 确认对话框
│   │   ├── LoginServerSettingsDialog.qml  # 登录服务器设置对话框
│   │   ├── WindowCaptionButtons.qml  # 窗口标题栏按钮组件
│   │   ├── PermissionButton.qml      # 权限控制按钮组件
│   │   ├── PermissionDeniedDialog.qml     # 权限拒绝对话框
│   │   ├── PermissionChipRow.qml     # 权限标签行组件
│   │   ├── PageAccessPlaceholder.qml # 页面访问占位符组件
│   │   ├── AttendanceStatusPieChart.qml   # 考勤状态饼图组件
│   │   ├── RecentPunchList.qml       # 最近打卡列表组件
│   │   ├── BarChart.qml              # 柱状图组件
│   │   ├── ChartLegend.qml           # 图表图例组件
│   │   ├── ChartContainer.qml        # 图表容器组件
│   │   ├── StatCard.qml              # 统计卡片组件
│   │   ├── PageHost.qml              # 页面托管容器组件
│   │   └── LineChart.qml             # 折线图组件
│   └── pages/
│       ├── PageDashboard.qml         # 仪表盘页面
│       ├── PagePerson.qml            # 人员管理页面
│       ├── PageDevice.qml            # 设备管理页面
│       ├── PageConfigDeploy.qml      # 配置部署页面
│       ├── PageAttendance.qml        # 考勤记录页面
│       ├── PageFace.qml              # 人脸数据管理页面
│       ├── PageUser.qml              # 用户管理页面
│       └── PageRbac.qml              # 角色权限管理页面
├── src/                              # C++ 源代码目录
│   ├── Models/                       # 数据模型
│   │   ├── Person.h / Person.cpp     # 人员模型
│   │   ├── attendancerecord.h / .cpp # 考勤记录模型
│   │   ├── device.h / device.cpp     # 设备模型
│   │   └── faceData.h / faceData.cpp # 人脸数据模型
│   ├── Protocol/
│   │   └── protocol.h                # 通信协议定义
│   ├── Network/
│   │   └── TcpConnectionManager.h / .cpp  # TCP 连接管理器
│   ├── Auth/
│   │   └── SessionManager.h / .cpp   # 会话管理器
│   ├── Person/
│   │   └── PersonServer.h / .cpp     # 人员管理服务
│   ├── Attendance/
│   │   └── AttendanceService.h / .cpp     # 考勤服务
│   ├── Device/
│   │   └── DeviceServer.h / .cpp     # 设备管理服务
│   ├── Config/
│   │   └── ConfigDeployServer.h / .cpp    # 配置部署服务
│   ├── Face/
│   │   └── FaceServer.h / .cpp       # 人脸识别服务
│   ├── User/
│   │   └── UserServer.h / .cpp       # 用户管理服务
│   ├── Rbac/
│   │   └── RbacServer.h / .cpp       # 权限管理服务
│   └── Event/
│       └── EventSubscriptionService.h / .cpp  # 事件订阅服务
└── out/                              # 构建输出目录
```

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        UI 层 (QML)                               │
├─────────────────────────────────────────────────────────────────┤
│  Main.qml  │  LoginPage  │  Pages (Dashboard/Person/Device...)  │
│  Components (Card/DataTable/Charts...)  │  Models (Logger/History) │
└───────────────────────────────┬─────────────────────────────────┘
                                │ QML Property Binding
┌───────────────────────────────▼─────────────────────────────────┐
│                      服务层 (C++ QObject)                        │
├─────────────────────────────────────────────────────────────────┤
│ SessionManager │ PersonServer │ DeviceServer │ AttendanceService │
│ FaceServer │ UserServer │ RbacServer │ ConfigDeployServer        │
│ EventSubscriptionService                                         │
└───────────────────────────────┬─────────────────────────────────┘
                                │ Qt Signal/Slot
┌───────────────────────────────▼─────────────────────────────────┐
│                     网络层 (C++)                                 │
├─────────────────────────────────────────────────────────────────┤
│                    TcpConnectionManager                          │
│         (TCP 长连接 / 心跳 / 重连 / 消息序列化)                   │
└───────────────────────────────┬─────────────────────────────────┘
                                │ TCP Socket
┌───────────────────────────────▼─────────────────────────────────┐
│                      后端服务器                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 核心设计原则

1. **前后端分离**: QML 负责 UI 展示与交互，C++ 负责业务逻辑与网络通信
2. **服务化设计**: 各功能模块独立封装为服务类，通过 `setTcpManager()` 注入网络管理器
3. **信号槽通信**: 使用 Qt 信号槽机制实现模块间解耦
4. **属性绑定**: QML 通过 `Q_PROPERTY` 与 C++ 数据双向绑定
5. **权限控制**: 完整的 RBAC（基于角色的访问控制）体系

---

## 4. 核心模块详解

### 4.1 网络通信模块 - TcpConnectionManager

#### 功能职责

- TCP 长连接管理（连接、断开、重连）
- 心跳保活机制
- 消息序列化与反序列化（JSON 行协议）
- 请求-响应匹配（基于 msgId）
- JWT Token 管理（accessToken / refreshToken）
- 二进制帧传输支持

#### 连接状态机

```
Disconnected ──connectToServer()──► Connecting
      ▲                                    │
      │                          onSocketConnected()
      │                                    ▼
      │                               Connected
      │                                    │
      │                            sendAuth() 成功
      │                                    ▼
      └────────disconnectFromServer()── Authenticated
```

#### 关键属性

| 属性 | 类型 | 说明 |
|-----|------|------|
| `connectionState` | ConnectionState | 连接状态（Disconnected/Connecting/Connected/Authenticated） |
| `isAuthenticated` | bool | 是否已认证 |
| `sessionToken` | QString | 会话令牌 |
| `accessToken` | QString | 访问令牌 |
| `refreshToken` | QString | 刷新令牌 |
| `roles` | QStringList | 当前用户角色列表 |
| `permissions` | QStringList | 当前用户权限列表 |

#### 配置参数

```cpp
struct ConnectionConfig {
    QString host           = "127.0.0.1";  // 服务器地址
    quint16 port           = 9527;         // 服务器端口
    QString clientId       = "admin_001";  // 客户端标识
    QString username;                      // 用户名
    QString password;                      // 密码
    int heartbeatSec       = 30;           // 心跳间隔（秒）
    int reconnectMaxAttempts = 10;         // 最大重连次数
    int reconnectBaseDelayMs  = 1000;      // 重连基础延迟（毫秒）
    int requestTimeoutMs      = 10000;     // 请求超时（毫秒）
};
```

### 4.2 会话管理模块 - SessionManager

#### 功能职责

- 用户登录/登出管理
- 会话状态维护
- 权限验证接口
- Token 自动刷新

#### 关键方法

| 方法 | 说明 |
|-----|------|
| `login(host, port, username, password, clientId)` | 用户登录 |
| `logout()` | 用户登出 |
| `hasPermission(permKey)` | 检查是否拥有指定权限 |
| `hasRole(roleKey)` | 检查是否拥有指定角色 |
| `refreshPermissions()` | 刷新权限列表 |
| `refreshTokens()` | 刷新访问令牌 |

### 4.3 人员管理模块 - PersonServer

#### 功能职责

- 人员信息的增删改查
- 支持按姓名、工号、部门、岗位筛选

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `createPerson(name, employeeId, department, position)` | person.create | 新增人员 |
| `queryPersons(name, employeeId, department, position, createdAt, updatedAt)` | person.read | 查询人员列表 |
| `updatePerson(employeeId, name, department, position)` | person.update | 更新人员信息 |
| `deletePerson(employeeId)` | person.delete | 删除人员 |

### 4.4 设备管理模块 - DeviceServer

#### 功能职责

- 考勤设备的注册与管理
- 设备状态监控（在线/离线/维护中）
- 设备指令下发
- 设备状态实时推送处理

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `createDevice(deviceId, deviceName, deviceKey, ipAddress, status)` | device.create | 新增设备 |
| `queryDevices(deviceId, deviceName, ipAddress)` | device.read | 查询设备列表 |
| `updateDevice(deviceId, deviceName, ipAddress, status)` | device.update | 更新设备信息 |
| `deleteDevice(deviceId)` | device.delete | 删除设备 |
| `sendCommand(deviceId, command, paramsJson)` | device.command | 发送设备指令 |

#### 设备状态

| 状态 | 说明 |
|-----|------|
| `online` | 在线 |
| `offline` | 离线 |
| `maintenance` | 维护中 |

### 4.5 考勤服务模块 - AttendanceService

#### 功能职责

- 考勤记录的查询与管理
- 考勤归档记录查询
- 数据导出（CSV）

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `query(idFilter, employeeId, checkTime, deviceId, status, receivedTime)` | attendance.read | 查询实时考勤 |
| `createRecord(employeeId, checkTime, status, deviceId)` | attendance.create | 新增考勤记录 |
| `updateRecord(locate, updates)` | attendance.update | 更新考勤记录 |
| `deleteRecord(criteria)` | attendance.delete | 删除考勤记录 |
| `queryArchive(...)` | attendance.archive.read | 查询归档考勤 |
| `deleteArchive(employeeId)` | attendance.archive.delete | 删除归档记录 |
| `exportToFile(fileUrl, content)` | attendance.read | 导出 CSV 文件 |

#### 考勤状态

| 状态 | 说明 | 颜色 |
|-----|------|------|
| `normal` | 正常 | 绿色 #16a34a |
| `late` | 迟到 | 橙色 #f97316 |
| `early` | 早退 | 紫色 #8b5cf6 |
| `absent` | 缺勤 | 红色 #ef4444 |
| `manual` | 补签 | 蓝色 #1565C0 |

### 4.6 人脸识别模块 - FaceServer

#### 功能职责

- 人脸特征数据管理
- 人脸照片上传与注册
- 支持从本地文件注册

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `queryFace(employeeId)` | face.read | 查询人脸元数据 |
| `registerFace(employeeId, photoBase64, overwrite)` | face.register | 注册人脸（Base64） |
| `registerFaceFromFile(employeeId, filePathOrUrl, overwrite)` | face.register | 从文件注册人脸 |
| `deleteFace(employeeId)` | face.delete | 删除人脸特征 |
| `clearRecords()` | - | 清空本地记录 |

### 4.7 用户管理模块 - UserServer

#### 功能职责

- 系统用户账号管理
- 用户密码管理

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `createUser(employeeId, password, name)` | user.create | 创建用户 |
| `queryUsers(employeeId, createdAt)` | user.read | 查询用户列表 |
| `updateUser(employeeId, name, password)` | user.update | 更新用户信息 |
| `deleteUser(employeeId)` | user.delete | 删除用户 |

### 4.8 权限管理模块 - RbacServer

#### 功能职责

- 角色管理（创建、查询、更新、删除）
- 权限查询
- 用户角色分配

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `queryRoles()` | super_admin | 查询所有角色 |
| `queryPermissions()` | super_admin | 查询所有权限 |
| `querySelfPermissions()` | - | 查询当前用户权限 |
| `queryUserRoles(userId)` | super_admin | 查询用户角色 |
| `createRole(roleKey, roleName, description)` | super_admin | 创建角色 |
| `updateRole(roleKey, fields)` | super_admin | 更新角色 |
| `deleteRole(roleKey)` | super_admin | 删除角色 |
| `assignUserRole(userId, roleKey)` | super_admin | 为用户分配角色 |
| `revokeUserRole(userId, roleKey)` | super_admin | 撤销用户角色 |

### 4.9 配置部署模块 - ConfigDeployServer

#### 功能职责

- 配置内容下发到指定设备
- 部署结果跟踪

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `deployConfig(targetDevices, configContent, configVersion, description)` | config.deploy | 下发配置 |

### 4.10 事件订阅模块 - EventSubscriptionService

#### 功能职责

- 订阅服务器推送事件
- 事件分发处理

#### API 方法

| 方法 | 权限 | 说明 |
|-----|------|------|
| `subscribe(topics)` | event.subscribe | 订阅事件主题 |
| `unsubscribe(topics)` | event.subscribe | 取消订阅 |

#### 支持的推送事件

| 事件类型 | 说明 |
|---------|------|
| `attendance.push` | 考勤打卡推送 |
| `device.status.push` | 设备状态变更推送 |

---

## 5. 通信协议

### 5.1 消息格式

所有消息采用 JSON 行协议，每条消息以换行符 `\n` 结尾。

#### 请求消息结构

```json
{
    "type": "person.query",
    "role": "admin",
    "msgId": "uuid-xxx",
    "ts": 1716633600000,
    "data": { ... }
}
```

#### 响应消息结构

```json
{
    "type": "person.query.response",
    "inReplyTo": "uuid-xxx",
    "code": 0,
    "msg": "success",
    "ts": 1716633600100,
    "data": { ... }
}
```

### 5.2 消息类型汇总

| 类别 | 消息类型 | 说明 |
|-----|---------|------|
| **认证** | `auth` | 登录认证请求 |
| | `auth_response` | 登录认证响应 |
| | `token.refresh` | Token 刷新请求 |
| | `token.refresh.response` | Token 刷新响应 |
| **心跳** | `heartbeat` | 心跳请求 |
| | `heartbeat_response` | 心跳响应 |
| **人员** | `person.create/query/update/delete` | 人员 CRUD |
| **考勤** | `attendance.create/query/update/delete` | 考勤 CRUD |
| | `attendance.archive.query/delete` | 归档考勤操作 |
| | `attendance.push` | 考勤打卡推送 |
| **设备** | `device.create/query/update/delete` | 设备 CRUD |
| | `device.command` | 设备指令 |
| | `device.status` | 设备状态 |
| | `device.auth.approve` | 设备授权审批 |
| **人脸** | `face.query/delete/register.header/register` | 人脸操作 |
| **用户** | `user.create/query/update/delete` | 用户 CRUD |
| **角色** | `role.create/query/update/delete` | 角色 CRUD |
| | `user.role.assign/revoke/query` | 用户角色操作 |
| **权限** | `permission.query/self` | 权限查询 |
| **配置** | `config.deploy` | 配置下发 |
| **订阅** | `subscribe/unsubscribe` | 事件订阅 |

### 5.3 错误码定义

| 错误码 | 常量名 | 说明 |
|-------|--------|------|
| 0 | kSuccess | 成功 |
| 1001 | kParseError | JSON 解析错误 |
| 1002 | kPayloadTooLarge | 负载过大 |
| 2001 | kNotAuthenticated | 未认证 |
| 2002 | kAuthFailed | 认证失败 |
| 2003 | kDuplicateSession | 重复会话 |
| 2004 | kTokenInvalid | Token 无效 |
| 2005 | kTokenExpired | Token 过期 |
| 2006 | kRefreshTokenInvalid | RefreshToken 无效 |
| 2007 | kInsufficientPermissions | 权限不足 |
| 3001 | kForbidden | 禁止访问 |
| 4000 | kBusinessValidation | 业务校验失败 |
| 4001 | kEmployeeNotFound | 员工不存在 |
| 5001 | kDeviceOffline | 设备离线 |
| 5002 | kForwardTimeout | 转发超时 |
| 6001 | kDuplicateKey | 重复键 |
| 6002 | kDbError | 数据库错误 |

---

## 6. 权限体系 (RBAC)

### 6.1 角色定义

| 角色键 | 显示名称 | 说明 |
|-------|---------|------|
| `super_admin` | 超级管理员 | 拥有所有权限，可管理 RBAC |
| `hr_admin` | 人事管理员 | 人员、考勤、人脸管理 |
| `device_admin` | 设备管理员 | 设备管理、配置下发 |
| `attendance_auditor` | 考勤审核员 | 考勤记录查看 |
| `viewer` | 只读观察者 | 仅查看，无修改权限 |

### 6.2 权限定义

| 权限键 | 显示名称 | 所属模块 |
|-------|---------|---------|
| `person.create` | 新增人员 | 人员管理 |
| `person.read` | 查询人员 | 人员管理 |
| `person.update` | 修改人员 | 人员管理 |
| `person.delete` | 删除人员 | 人员管理 |
| `attendance.create` | 录入考勤 | 考勤管理 |
| `attendance.read` | 查询考勤 | 考勤管理 |
| `attendance.update` | 修改考勤 | 考勤管理 |
| `attendance.delete` | 删除考勤 | 考勤管理 |
| `attendance.archive.read` | 查询归档考勤 | 考勤管理 |
| `attendance.archive.delete` | 删除归档考勤 | 考勤管理 |
| `device.create` | 新增设备 | 设备管理 |
| `device.read` | 查询设备 | 设备管理 |
| `device.update` | 修改设备 | 设备管理 |
| `device.delete` | 删除设备 | 设备管理 |
| `device.command` | 设备指令 | 设备管理 |
| `config.deploy` | 配置下发 | 配置管理 |
| `user.create` | 新增用户 | 用户管理 |
| `user.read` | 查询用户 | 用户管理 |
| `user.update` | 修改用户 | 用户管理 |
| `user.delete` | 删除用户 | 用户管理 |
| `face.register` | 注册人脸 | 人脸管理 |
| `face.read` | 查询人脸 | 人脸管理 |
| `face.delete` | 删除人脸 | 人脸管理 |
| `event.subscribe` | 订阅事件 | 事件管理 |

### 6.3 页面访问权限

| 页面 | 所需权限 |
|-----|---------|
| 仪表盘 (dashboard) | 无限制（登录即可） |
| 人员管理 (person) | `person.read` |
| 设备管理 (device) | `device.read` |
| 配置下发 (configDeploy) | `config.deploy` |
| 考勤记录 (attendance) | `attendance.read` 或 `attendance.archive.read` |
| 人脸库 (face) | `face.read` |
| 用户账号 (user) | `user.read` |
| 用户权限 (rbac) | `super_admin` 角色 |

---

## 7. UI 组件库

### 7.1 基础组件

| 组件 | 文件 | 说明 |
|-----|------|------|
| `Card` | Card.qml | 卡片容器，支持标题、副标题、右侧操作区 |
| `BusyOverlay` | BusyOverlay.qml | 全屏加载遮罩 |
| `BadgeStatus` | BadgeStatus.qml | 状态徽章，支持多种颜色状态 |
| `LabeledField` | LabeledField.qml | 带标签的表单字段容器 |
| `ToolBarRow` | ToolBarRow.qml | 页面标题栏，支持操作按钮 |
| `ConfirmDialog` | ConfirmDialog.qml | 确认对话框 |

### 7.2 数据组件

| 组件 | 文件 | 说明 |
|-----|------|------|
| `DataTable` | DataTable.qml | 通用数据表格，支持列定义、行点击、格式化 |
| `JsonEditor` | JsonEditor.qml | JSON 编辑器，支持语法高亮 |
| `JsonViewer` | JsonViewer.qml | JSON 只读查看器 |

### 7.3 权限组件

| 组件 | 文件 | 说明 |
|-----|------|------|
| `PermissionButton` | PermissionButton.qml | 权限控制按钮，无权限时禁用或弹出提示 |
| `PermissionChipRow` | PermissionChipRow.qml | 权限标签行展示 |
| `PermissionDeniedDialog` | PermissionDeniedDialog.qml | 权限拒绝对话框 |
| `PageAccessPlaceholder` | PageAccessPlaceholder.qml | 无权限页面占位符 |

### 7.4 图表组件

| 组件 | 文件 | 说明 |
|-----|------|------|
| `AttendanceStatusPieChart` | AttendanceStatusPieChart.qml | 考勤状态饼图 |
| `BarChart` | BarChart.qml | 柱状图 |
| `ChartContainer` | ChartContainer.qml | 图表容器 |
| `ChartLegend` | ChartLegend.qml | 图表图例 |
| `StatCard` | StatCard.qml | 统计卡片 |

### 7.5 导航组件

| 组件 | 文件 | 说明 |
|-----|------|------|
| `SideBar` | SideBar.qml | 侧边栏导航，支持分组、权限过滤 |
| `StatusBar` | StatusBar.qml | 顶部状态栏，显示连接状态、用户信息 |
| `PageHost` | PageHost.qml | 页面托管容器，管理页面切换 |
| `WindowCaptionButtons` | WindowCaptionButtons.qml | 无边框窗口标题栏按钮 |

---

## 8. 主题系统

### 8.1 颜色系统

```qml
// 主色调（企业蓝）
primary:         "#1D6BFF"
primaryHover:    "#3B82FF"
primarySubtle:   "#DBEAFE"

// 背景色
bg:              "#F2F4F7"    // 页面背景
surface:         "#FFFFFF"    // 卡片背景
surfaceAlt:      "#EEF1F6"    // 备选背景
sideBar:         "#E8EBF1"    // 侧边栏背景

// 文字色
text:            "#1F2937"    // 主文字
textMuted:       "#3D4551"    // 次要文字
textSubtle:      "#6B7280"    // 提示文字

// 状态色
success:         "#0D8A4C"    // 成功/在线
warning:         "#9A3E04"    // 警告
danger:          "#C62828"    // 错误/离线
info:            "#1565C0"    // 信息

// 考勤状态色
pieNormal:       "#16a34a"    // 正常
pieLate:         "#f97316"    // 迟到
pieEarly:        "#8b5cf6"    // 早退
pieAbsent:       "#ef4444"    // 缺勤
```

### 8.2 间距系统

```qml
spacingXs:  4   // 极小间距
spacingSm:  6   // 小间距
spacingMd:  10  // 中等间距
spacingLg:  16  // 大间距
spacingXl:  24  // 超大间距
```

### 8.3 字号系统

```qml
fontXs:   11
fontSm:   12
fontMd:   13
fontLg:   15
fontXl:   18
fontXxl:  22
```

### 8.4 圆角系统

```qml
radiusSm:  4
radiusMd:  6
radiusLg:  10
```

---

## 9. 构建与运行

### 9.1 环境要求

- Qt 6.8+
- CMake 3.16+
- C++17 编译器
- Windows: MSVC 2019+ 或 MinGW
- macOS: Xcode 13+ 或 Clang

### 9.2 构建步骤

```bash
# 创建构建目录
mkdir build
cd build

# 配置项目
cmake .. -DCMAKE_BUILD_TYPE=Debug

# 编译
cmake --build .

# 运行
./appAttendanceAdmin  # Linux/macOS
appAttendanceAdmin.exe  # Windows
```

### 9.3 Qt Creator 构建

1. 打开 Qt Creator
2. 文件 → 打开文件或项目 → 选择 `CMakeLists.txt`
3. 配置项目（选择 Qt 6.8+ Kit）
4. 构建 → 构建项目
5. 运行

---

## 10. 使用指南

### 10.1 登录流程

1. 启动应用程序
2. 输入用户名/工号和密码
3. 点击登录按钮
4. 登录成功后自动跳转到仪表盘

### 10.2 服务器配置

点击登录页面右上角的齿轮图标，可配置：
- 服务器地址
- 服务器端口

### 10.3 功能导航

登录后，左侧边栏显示可访问的功能模块（根据权限自动过滤）：

- **仪表盘**: 查看员工人数、今日打卡、设备连接数统计
- **人员管理**: 人员信息的增删改查
- **设备管理**: 考勤设备管理
- **配置下发**: 向设备下发配置
- **考勤记录**: 查询和管理考勤数据
- **人脸库**: 人脸特征数据管理
- **用户账号**: 系统用户管理
- **用户权限**: 角色权限管理（仅超级管理员）

### 10.4 权限说明

- 无权限访问的页面不会显示在导航中
- 无权限执行的操作按钮会被禁用或隐藏
- 尝试访问无权限功能会弹出权限提示

---

## 11. 错误处理

### 11.1 连接错误

- 服务器未启动：显示"未连接服务器"
- 网络中断：自动尝试重连（最多 10 次）
- 认证失败：显示具体错误信息

### 11.2 业务错误

- 操作失败时显示错误提示 Toast
- 错误信息通过 `ErrorCatalog` 映射为友好提示
- 调用历史记录所有请求和响应

---

## 12. 日志与调试

### 12.1 日志模型 (Logger)

```qml
Logger.info("信息日志")
Logger.ok("成功日志")
Logger.warn("警告日志")
Logger.error("错误日志")
```

### 12.2 调用历史 (History)

记录所有 TCP 请求和响应，便于调试：
- 请求方向（SEND/RECV）
- 消息类型
- 完整载荷
- 执行结果

### 12.3 Qt 消息处理

应用程序安装了自定义消息处理器，所有 Qt 日志输出到：
- Windows: OutputDebugString + stderr
- 其他平台: stderr

---

## 13. 扩展开发

### 13.1 添加新页面

1. 在 `ui/pages/` 创建新的 QML 文件
2. 在 `CMakeLists.txt` 的 `QML_FILES` 中添加文件
3. 在 `ui/components/PageHost.qml` 中添加页面加载逻辑
4. 在 `ui/models/PermissionCatalog.qml` 的 `allNavItems` 中添加导航项

### 13.2 添加新服务

1. 在 `src/` 下创建新目录和类文件
2. 继承 `QObject`，使用 `Q_PROPERTY` 暴露属性
3. 实现 `setTcpManager()` 方法
4. 在 `main.cpp` 中实例化并注入到 QML 上下文

### 13.3 添加新协议

1. 在 `src/Protocol/protocol.h` 中定义消息类型常量
2. 在对应服务类中实现请求方法
3. 处理响应并发出信号

---

## 14. 版本信息

- **当前版本**: 0.1
- **Qt 版本**: 6.8+
- **最后更新**: 2026-05-25

---

## 15. 附录

### 15.1 窗口尺寸

| 窗口状态 | 宽度 | 高度 |
|---------|------|------|
| 登录窗口 | 400 | 542 |
| 主窗口 | 1480 | 920 |

### 15.2 默认配置

| 配置项 | 默认值 |
|-------|--------|
| 服务器地址 | 127.0.0.1 |
| 服务器端口 | 9527 |
| 客户端 ID | admin_001 |
| 心跳间隔 | 30 秒 |
| 请求超时 | 10 秒 |
| 最大重连次数 | 10 次 |
