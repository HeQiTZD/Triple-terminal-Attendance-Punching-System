# 考勤系统管理端开发指导文档

本文档面向管理端（Admin Client）的开发者，详细说明如何与 attendanceServer 进行通信，包含连接建立、认证流程、全部 API 的请求/响应格式、错误处理、以及最佳实践。

---

## 目录

1. [通信基础](#1-通信基础)
2. [连接与认证](#2-连接与认证)
3. [消息信封格式](#3-消息信封格式)
4. [心跳保活](#4-心跳保活)
5. [人员管理 API](#5-人员管理-api)
6. [考勤记录管理 API](#6-考勤记录管理-api)
7. [考勤归档管理 API](#7-考勤归档管理-api)
8. [设备管理 API](#8-设备管理-api)
9. [用户账号管理 API](#9-用户账号管理-api)
10. [人脸数据管理 API](#10-人脸数据管理-api)
11. [人脸特征注册 API](#11-人脸特征注册-api)
12. [RBAC 角色管理 API](#12-rbac-角色管理-api)
13. [用户角色管理 API](#13-用户角色管理-api)
14. [权限查询 API](#14-权限查询-api)
15. [设备指令转发](#15-设备指令转发)
16. [事件订阅与推送](#16-事件订阅与推送)
17. [错误码参考](#17-错误码参考)
18. [RBAC 权限参考](#18-rbac-权限参考)
19. [开发最佳实践](#19-开发最佳实践)

---

## 1. 通信基础

### 1.1 传输协议

- **传输层**：TCP 长连接
- **默认端口**：由服务端配置决定（参见 `ServerConfig`）
- **编码**：UTF-8

### 1.2 消息分帧

所有消息采用 **JSON 行分帧** —— 每条消息为一行紧凑 JSON 文本，以 `\n` 结尾。

```
{"type":"auth","role":"admin","from":"admin_001","msgId":"1","data":{...}}\n
```

**限制**：

- 单行 JSON 最大长度：**1 MB**（超出将断开连接）
- 二进制帧最大 payload：**16 MB**

### 1.3 二进制帧

在考勤照片上报场景下，JSON 头消息之后紧跟一个二进制帧：

```
[4 字节大端长度前缀][二进制 payload]
```

服务端在解析到 JSON 头中的 `payloadLength` 后自动切换为二进制接收模式。

> **注意**：人脸特征注册已不再使用二进制帧方式，改用单条 JSON 消息携带 Base64 编码照片（参见 §11）。

---

## 2. 连接与认证

### 2.1 认证流程

管理端连接后的第一条消息**必须**是 `auth`，否则所有业务消息将被拒绝（返回 `2001 未认证`）。

**请求**：

```json
{
  "type": "auth",
  "role": "admin",
  "from": "admin_001",
  "msgId": "msg-1",
  "ts": 1715420000000,
  "data": {
    "username": "EMP001",
    "password": "your_password"
  }
}
```


| 字段              | 类型     | 必填  | 说明                               |
| --------------- | ------ | --- | -------------------------------- |
| `type`          | string | 是   | 固定 `"auth"`                      |
| `role`          | string | 是   | 固定 `"admin"`                     |
| `from`          | string | 是   | 管理端标识（操作员 ID），用于会话绑定             |
| `msgId`         | string | 是   | 消息唯一 ID，服务端在响应中通过 `inReplyTo` 回引 |
| `data.username` | string | 是   | 用户的 `employee_id`（工号）            |
| `data.password` | string | 是   | 密码明文（传输层加密由网络层保证）                |


**成功响应**：

```json
{
  "type": "auth_response",
  "role": "server",
  "from": "server",
  "to": "admin_001",
  "inReplyTo": "msg-1",
  "ts": 1715420000123,
  "code": 0,
  "msg": "ok",
  "data": {
    "sessionToken": "...",
    "serverTime": 1715420000123,
    "heartbeatSec": 30,
    "roles": ["super_admin"],
    "permissions": ["person.create", "person.read", "..."]
  }
}
```

**关键返回字段**：


| 字段                  | 说明                     |
| ------------------- | ---------------------- |
| `data.heartbeatSec` | 心跳间隔秒数，管理端需按此频率发送心跳    |
| `data.roles`        | 当前用户的角色列表              |
| `data.permissions`  | 当前用户的聚合权限列表（所有角色权限的并集） |


### 2.2 认证失败

认证失败时返回 `error` 消息：

```json
{
  "type": "error",
  "role": "server",
  "from": "server",
  "to": "admin_001",
  "inReplyTo": "msg-1",
  "ts": 1715420000123,
  "code": 2002,
  "msg": "auth failed"
}
```

### 2.3 登录锁定

- 连续 **5 次**认证失败后，账号锁定 **15 分钟**
- 锁定期间所有登录尝试返回 `"account locked"`
- 登录成功后失败计数重置

---

## 3. 消息信封格式

所有消息遵循统一的信封结构：

```json
{
  "type": "message_type",
  "role": "admin",
  "from": "admin_001",
  "to": "server",
  "msgId": "unique-id",
  "inReplyTo": "",
  "ts": 1715420000000,
  "code": 0,
  "msg": "",
  "data": { ... }
}
```


| 字段          | 类型     | 说明                             |
| ----------- | ------ | ------------------------------ |
| `type`      | string | 消息类型标识，点分命名（如 `person.create`） |
| `role`      | string | 发送方角色：管理端固定为 `"admin"`         |
| `from`      | string | 发送方标识                          |
| `to`        | string | 目标方标识（可选）                      |
| `msgId`     | string | 消息唯一 ID（建议使用 UUID）             |
| `inReplyTo` | string | 回复关联的 msgId（仅响应时使用）            |
| `ts`        | int64  | UTC 毫秒时间戳                      |
| `code`      | int    | 状态码，`0` 为成功                    |
| `msg`       | string | 状态描述                           |
| `data`      | object | 业务数据                           |


**服务端响应**的信封中 `role` 为 `"server"`，`inReplyTo` 为请求的 `msgId`。

---

## 4. 心跳保活

认证成功后，管理端需要按 `heartbeatSec` 间隔周期性发送心跳。超时未收到心跳的连接将被服务端主动断开。

**请求**：

```json
{
  "type": "heartbeat",
  "role": "admin",
  "from": "admin_001",
  "msgId": "hb-1",
  "ts": 1715420030000
}
```

**响应**：

```json
{
  "type": "heartbeat_response",
  "role": "server",
  "from": "server",
  "to": "admin_001",
  "inReplyTo": "hb-1",
  "ts": 1715420030001,
  "code": 0,
  "msg": "ok"
}
```

> 超时阈值 = `heartbeatSec × heartbeat_grace_multiplier`（默认乘数由服务端配置）。建议管理端以 `heartbeatSec` 的 **80%** 为发送间隔，留出余量。

---

## 5. 人员管理 API

### 5.1 新增人员 `person.create`

**所需权限**：`person.create`

**请求**：

```json
{
  "type": "person.create",
  "role": "admin",
  "from": "admin_001",
  "msgId": "p-c-1",
  "data": {
    "name": "张三",
    "employeeId": "EMP001",
    "department": "技术部",
    "position": "工程师"
  }
}
```


| 字段           | 类型     | 必填  | 说明     |
| ------------ | ------ | --- | ------ |
| `name`       | string | 是   | 姓名     |
| `employeeId` | string | 是   | 工号（唯一） |
| `department` | string | 是   | 部门     |
| `position`   | string | 是   | 职位     |


**成功响应**：

```json
{
  "type": "person.create.response",
  "code": 0,
  "msg": "ok",
  "inReplyTo": "p-c-1",
  "data": {}
}
```

### 5.2 查询人员 `person.query`

**所需权限**：`person.read`

**请求**：

```json
{
  "type": "person.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "p-q-1",
  "data": {
    "name": "",
    "employeeId": "EMP001",
    "department": "",
    "position": "",
    "createdAt": "",
    "updatedAt": ""
  }
}
```

所有查询条件均为可选，空字符串表示不过滤。

**成功响应**：

```json
{
  "type": "person.query.response",
  "code": 0,
  "msg": "ok",
  "inReplyTo": "p-q-1",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "张三",
        "employeeId": "EMP001",
        "department": "技术部",
        "position": "工程师",
        "createdAt": "2025-01-01 09:00:00",
        "updatedAt": "2025-01-01 09:00:00"
      }
    ]
  }
}
```

### 5.3 修改人员 `person.update`

**所需权限**：`person.update`

**请求**：

```json
{
  "type": "person.update",
  "role": "admin",
  "from": "admin_001",
  "msgId": "p-u-1",
  "data": {
    "employeeId": "EMP001",
    "name": "张三（新）",
    "department": "产品部",
    "position": ""
  }
}
```


| 字段           | 类型     | 必填  | 说明         |
| ------------ | ------ | --- | ---------- |
| `employeeId` | string | 是   | 定位人员的工号    |
| `name`       | string | 否   | 新姓名（非空则更新） |
| `department` | string | 否   | 新部门（非空则更新） |
| `position`   | string | 否   | 新职位（非空则更新） |


至少需要提供一个非空的更新字段。

### 5.4 删除人员 `person.delete`

**所需权限**：`person.delete`

**请求**：

```json
{
  "type": "person.delete",
  "role": "admin",
  "from": "admin_001",
  "msgId": "p-d-1",
  "data": {
    "employeeId": "EMP001"
  }
}
```

---

## 6. 考勤记录管理 API

### 6.1 新增考勤 `attendance.create`

**所需权限**：`attendance.create`

**请求**：

```json
{
  "type": "attendance.create",
  "role": "admin",
  "from": "admin_001",
  "msgId": "a-c-1",
  "data": {
    "employeeId": "EMP001",
    "checkTime": "2025-05-10 09:00:00",
    "status": "normal",
    "deviceId": "DEV001"
  }
}
```


| 字段           | 类型     | 必填  | 说明                                     |
| ------------ | ------ | --- | -------------------------------------- |
| `employeeId` | string | 是   | 工号                                     |
| `checkTime`  | string | 是   | 打卡时间（`YYYY-MM-DD HH:MM:SS`）            |
| `status`     | string | 是   | 状态（`normal`/`late`/`early`/`absent` 等） |
| `deviceId`   | string | 否   | 设备 ID                                  |


### 6.2 查询考勤 `attendance.query`

**所需权限**：`attendance.read`

**请求**：

```json
{
  "type": "attendance.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "a-q-1",
  "data": {
    "id": null,
    "employeeId": "EMP001",
    "checkTime": "",
    "deviceId": "",
    "status": "",
    "receivedTime": ""
  }
}
```

所有条件均为可选。`id` 可传整数精确匹配。

**成功响应**：

```json
{
  "type": "attendance.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "employeeId": "EMP001",
        "checkTime": "2025-05-10 09:00:00",
        "deviceId": "DEV001",
        "status": "normal",
        "receivedTime": "2025-05-10 09:00:01"
      }
    ]
  }
}
```

### 6.3 修改考勤 `attendance.update`

**所需权限**：`attendance.update`

采用 `locate` + `updates` 双层结构，先定位记录，再指定更新字段：

```json
{
  "type": "attendance.update",
  "role": "admin",
  "from": "admin_001",
  "msgId": "a-u-1",
  "data": {
    "locate": {
      "id": 1,
      "employeeId": "",
      "checkTime": "",
      "deviceId": "",
      "status": "",
      "receivedTime": ""
    },
    "updates": {
      "checkTime": "",
      "deviceId": "",
      "status": "late",
      "receivedTime": ""
    }
  }
}
```

- `locate` 中至少提供一个非空条件
- `updates` 中至少提供一个非空字段

### 6.4 删除考勤 `attendance.delete`

**所需权限**：`attendance.delete`

```json
{
  "type": "attendance.delete",
  "role": "admin",
  "from": "admin_001",
  "msgId": "a-d-1",
  "data": {
    "id": 1,
    "employeeId": "",
    "checkTime": "",
    "deviceId": "",
    "status": "",
    "receivedTime": ""
  }
}
```

至少提供一个非空条件，防止误删全表。

---

## 7. 考勤归档管理 API

### 7.1 查询归档 `attendance.archive.query`

**所需权限**：`attendance.archive.read`

```json
{
  "type": "attendance.archive.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "aa-q-1",
  "data": {
    "id": null,
    "employeeId": "",
    "name": "",
    "department": "",
    "position": "",
    "checkTime": "",
    "deviceId": "",
    "status": "",
    "receivedTime": "",
    "archivedAt": "",
    "archiveReason": ""
  }
}
```

**成功响应**：

```json
{
  "type": "attendance.archive.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "employeeId": "EMP001",
        "name": "张三",
        "department": "技术部",
        "position": "工程师",
        "checkTime": "2025-05-10 09:00:00",
        "deviceId": "DEV001",
        "status": "normal",
        "receivedTime": "2025-05-10 09:00:01",
        "archivedAt": "2025-06-01 00:00:00",
        "archiveReason": "monthly_archive"
      }
    ]
  }
}
```

### 7.2 删除归档 `attendance.archive.delete`

**所需权限**：`attendance.archive.delete`

```json
{
  "type": "attendance.archive.delete",
  "role": "admin",
  "from": "admin_001",
  "msgId": "aa-d-1",
  "data": {
    "employeeId": "EMP001"
  }
}
```

---

## 8. 设备管理 API

### 8.1 新增设备 `device.create`

**所需权限**：`device.create`

```json
{
  "type": "device.create",
  "role": "admin",
  "from": "admin_001",
  "msgId": "d-c-1",
  "data": {
    "deviceId": "DEV001",
    "deviceName": "前台考勤机",
    "ipAddress": "192.168.1.100",
    "lastOnline": "",
    "status": "offline"
  }
}
```


| 字段           | 类型     | 必填  | 说明     |
| ------------ | ------ | --- | ------ |
| `deviceId`   | string | 是   | 设备唯一标识 |
| `deviceName` | string | 是   | 设备名称   |
| `ipAddress`  | string | 否   | IP 地址  |
| `lastOnline` | string | 否   | 最后在线时间 |
| `status`     | string | 否   | 状态     |


### 8.2 查询设备 `device.query`

**所需权限**：`device.read`

```json
{
  "type": "device.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "d-q-1",
  "data": {
    "deviceId": "",
    "deviceName": "",
    "ipAddress": "",
    "lastOnline": ""
  }
}
```

**成功响应**：

```json
{
  "type": "device.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "deviceId": "DEV001",
        "deviceName": "前台考勤机",
        "ipAddress": "192.168.1.100",
        "lastOnline": "2025-05-10 18:00:00",
        "status": "online"
      }
    ]
  }
}
```

### 8.3 修改设备 `device.update`

**所需权限**：`device.update`

```json
{
  "type": "device.update",
  "role": "admin",
  "from": "admin_001",
  "msgId": "d-u-1",
  "data": {
    "deviceId": "DEV001",
    "deviceName": "前台考勤机（新）",
    "ipAddress": "",
    "lastOnline": "",
    "status": ""
  }
}
```

`deviceId` 为必填（用于定位），其余至少一个非空。

### 8.4 删除设备 `device.delete`

**所需权限**：`device.delete`

```json
{
  "type": "device.delete",
  "role": "admin",
  "from": "admin_001",
  "msgId": "d-d-1",
  "data": {
    "deviceId": "DEV001"
  }
}
```

---

## 9. 用户账号管理 API

### 9.1 新增用户 `user.create`

**所需权限**：`user.create`

```json
{
  "type": "user.create",
  "role": "admin",
  "from": "admin_001",
  "msgId": "u-c-1",
  "data": {
    "employeeId": "EMP002",
    "password": "SecurePass123",
    "name": "李四"
  }
}
```


| 字段           | 类型     | 必填  | 说明                 |
| ------------ | ------ | --- | ------------------ |
| `employeeId` | string | 是   | 登录用户名（对应工号）        |
| `password`   | string | 是   | 密码（≥8 字符，至少含字母和数字） |
| `name`       | string | 否   | 显示名称               |


**密码强度要求**：

- 长度 ≥ 8 个字符
- 至少包含字母和数字

密码存储使用 SHA-256 + 随机盐值哈希。

### 9.2 查询用户 `user.query`

**所需权限**：`user.read`

```json
{
  "type": "user.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "u-q-1",
  "data": {
    "employeeId": "",
    "createdAt": ""
  }
}
```

**成功响应**（注意：响应中**不包含密码**字段）：

```json
{
  "type": "user.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "employeeId": "EMP001",
        "name": "张三",
        "createdAt": "2025-01-01 09:00:00",
        "lastLoginTime": "2025-05-10 08:00:00"
      }
    ]
  }
}
```

### 9.3 修改用户 `user.update`

**所需权限**：`user.update`

```json
{
  "type": "user.update",
  "role": "admin",
  "from": "admin_001",
  "msgId": "u-u-1",
  "data": {
    "employeeId": "EMP002",
    "name": "李四（新）",
    "password": "NewPass456"
  }
}
```

- `employeeId` 必填用于定位
- `name` 和 `password` 至少提供一个
- 修改密码同样需要满足强度要求

### 9.4 删除用户 `user.delete`

**所需权限**：`user.delete`

```json
{
  "type": "user.delete",
  "role": "admin",
  "from": "admin_001",
  "msgId": "u-d-1",
  "data": {
    "employeeId": "EMP002"
  }
}
```

**保护机制**：如果目标用户是系统中唯一的 `super_admin`，删除将被拒绝。

---

## 10. 人脸数据管理 API

### 10.1 查询人脸 `face.query`

**所需权限**：`face.read`

```json
{
  "type": "face.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "f-q-1",
  "data": {
    "employeeId": "EMP001"
  }
}
```

**成功响应**：

```json
{
  "type": "face.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "found": true,
    "employeeId": "EMP001",
    "featureSize": 512,
    "createdAt": "2025-01-15 10:00:00",
    "updatedAt": "2025-01-15 10:00:00"
  }
}
```

若未找到则 `data.found` 为 `false`。

### 10.2 删除人脸 `face.delete`

**所需权限**：`face.delete`

```json
{
  "type": "face.delete",
  "role": "admin",
  "from": "admin_001",
  "msgId": "f-d-1",
  "data": {
    "employeeId": "EMP001"
  }
}
```

---

## 11. 人脸特征注册 API

**所需权限**：`face.register`

人脸特征注册采用**单条 JSON 消息**完成，管理端上传一张员工正面照片（Base64 编码），服务端自动完成人脸检测和特征提取。

### 请求

```json
{
  "type": "face.register",
  "role": "admin",
  "from": "admin_001",
  "msgId": "fr-1",
  "ts": 1715644800000,
  "data": {
    "employeeId": "EMP001",
    "photoBase64": "/9j/4AAQSkZJRgABAQAAAQABAAD/2wBDA...",
    "overwrite": false
  }
}
```


| 字段            | 类型      | 必填  | 说明                                  |
| ------------- | ------- | --- | ----------------------------------- |
| `employeeId`  | string  | 是   | 目标人员工号                              |
| `photoBase64` | string  | 是   | 人脸照片的 Base64 编码（支持 JPEG/PNG/BMP 格式） |
| `overwrite`   | boolean | 否   | 是否覆盖已存在的人脸数据，默认 false              |


### 成功响应

```json
{
  "type": "face.register.response",
  "role": "server",
  "from": "server",
  "to": "admin_001",
  "inReplyTo": "fr-1",
  "code": 0,
  "msg": "ok",
  "data": {
    "employeeId": "EMP001",
    "featureSize": 2048,
    "faceRect": {
      "left": 120,
      "top": 80,
      "right": 280,
      "bottom": 240
    },
    "isNew": true
  }
}
```


| 字段               | 类型      | 说明                       |
| ---------------- | ------- | ------------------------ |
| `employeeId`     | string  | 员工工号                     |
| `featureSize`    | int     | 提取到的特征向量长度（字节）           |
| `faceRect`       | object  | 检测到的人脸位置（left/top/right/bottom 像素坐标） |
| `isNew`          | boolean | 是否为新注册（true=首次，false=覆盖更新）  |


### 错误响应

与所有业务消息一致，错误响应的 `type` 为 `"face.register.response"`，`code` 为非零值：

```json
{
  "type": "face.register.response",
  "inReplyTo": "fr-1",
  "code": 4007,
  "msg": "face data already exists for this employee, set overwrite=true to replace"
}
```

### 特有错误码

| 错误码 | 说明 |
|--------|------|
| 4001 | 未检测到人脸 — 照片中无人脸或人脸不清晰 |
| 4002 | 检测到多张人脸 — 照片中只能有一人 |
| 4003 | 人脸尺寸不足 — 人脸区域小于 80 像素 |
| 4004 | 特征提取失败 — 图片质量不足以提取特征 |
| 4005 | 图片格式错误 — Base64 解码失败或图片损坏 |
| 4006 | 员工不存在 — 指定的 employeeId 在系统中不存在 |
| 4007 | 人脸已存在 — 该员工已有人脸数据且未设置 overwrite=true |

### 客户端实现注意事项

1. **图片编码**：将图片文件读取为字节数组后 Base64 编码，放入 `photoBase64` 字段
2. **图片要求**：单人、正面、光线充足、人脸清晰可见，建议分辨率 ≥ 200×200
3. **大小限制**：Base64 字符串（`photoBase64`）长度建议 ≤ 2MB，超限可能触发 `4000` 参数错误
4. **覆盖确认**：如果员工已有人脸数据，前端应提示用户确认后再设置 `overwrite: true`
5. **替代旧接口**：本接口完全替代原有的 `face.register.header` + 二进制帧方式，旧接口已移除

---

## 12. RBAC 角色管理 API

> 角色的创建、修改、删除操作**仅限 `super_admin` 角色**，查询仅需 `user.read` 权限。

### 12.1 新增角色 `role.create`

**所需角色**：`super_admin`

```json
{
  "type": "role.create",
  "role": "admin",
  "from": "admin_001",
  "msgId": "r-c-1",
  "data": {
    "roleKey": "custom_role",
    "roleName": "自定义角色",
    "description": "用于特定场景的角色"
  }
}
```


| 字段            | 类型     | 必填  | 说明                    |
| ------------- | ------ | --- | --------------------- |
| `roleKey`     | string | 是   | 角色唯一标识（建议 snake_case） |
| `roleName`    | string | 是   | 角色显示名称                |
| `description` | string | 否   | 角色描述                  |


### 12.2 查询角色 `role.query`

**所需权限**：`user.read`

```json
{
  "type": "role.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "r-q-1",
  "data": {}
}
```

**成功响应**：

```json
{
  "type": "role.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "roleKey": "super_admin",
        "roleName": "超级管理员",
        "description": "拥有全部权限",
        "isSystem": true,
        "createdAt": "2025-01-01 00:00:00",
        "updatedAt": "2025-01-01 00:00:00"
      }
    ]
  }
}
```

### 12.3 修改角色 `role.update`

**所需角色**：`super_admin`

```json
{
  "type": "role.update",
  "role": "admin",
  "from": "admin_001",
  "msgId": "r-u-1",
  "data": {
    "roleKey": "custom_role",
    "roleName": "自定义角色（新名称）",
    "description": "更新后的描述",
    "permissions": ["person.read", "attendance.read", "device.read"]
  }
}
```

- `roleKey` 必填用于定位
- `roleName` 和 `description` 可选（非空则更新）
- `permissions` 数组可选——传入时将 **整体替换** 该角色的权限集合（注意：不是增量操作）

### 12.4 删除角色 `role.delete`

**所需角色**：`super_admin`

```json
{
  "type": "role.delete",
  "role": "admin",
  "from": "admin_001",
  "msgId": "r-d-1",
  "data": {
    "roleKey": "custom_role"
  }
}
```

**保护机制**：系统内置角色（`is_system = true`）不可删除。

---

## 13. 用户角色管理 API

> 分配和撤销操作**仅限 `super_admin` 角色**，查询仅需 `user.read` 权限。

### 13.1 分配角色 `user.role.assign`

**所需角色**：`super_admin`

```json
{
  "type": "user.role.assign",
  "role": "admin",
  "from": "admin_001",
  "msgId": "ur-a-1",
  "data": {
    "userId": 2,
    "roleKey": "hr_admin"
  }
}
```


| 字段        | 类型     | 必填  | 说明                             |
| --------- | ------ | --- | ------------------------------ |
| `userId`  | int    | 是   | 目标用户的数据库 ID（从 `user.query` 获取） |
| `roleKey` | string | 是   | 角色的 `role_key`                 |


分配成功后，如果目标用户在线，其权限集将**实时热更新**，无需重新登录。

### 13.2 撤销角色 `user.role.revoke`

**所需角色**：`super_admin`

```json
{
  "type": "user.role.revoke",
  "role": "admin",
  "from": "admin_001",
  "msgId": "ur-r-1",
  "data": {
    "userId": 2,
    "roleKey": "hr_admin"
  }
}
```

**保护机制**：

- 不允许撤销自身的 `super_admin` 角色
- 如果系统中仅剩一个 `super_admin` 用户，不允许撤销其 `super_admin` 角色

### 13.3 查询用户角色 `user.role.query`

**所需权限**：`user.read`

```json
{
  "type": "user.role.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "ur-q-1",
  "data": {
    "userId": 2
  }
}
```

**成功响应**：

```json
{
  "type": "user.role.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "roles": ["hr_admin", "viewer"]
  }
}
```

---

## 14. 权限查询 API

### 14.1 查询全部权限 `permission.query`

**所需权限**：`user.read`

查询系统中定义的所有权限项。

```json
{
  "type": "permission.query",
  "role": "admin",
  "from": "admin_001",
  "msgId": "perm-q-1",
  "data": {}
}
```

**成功响应**：

```json
{
  "type": "permission.query.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "records": [
      {
        "id": 1,
        "permKey": "person.create",
        "permName": "创建人员",
        "resource": "person",
        "description": "..."
      }
    ]
  }
}
```

### 14.2 查询当前用户权限 `permission.self`

**所需权限**：仅需已认证的管理端身份（无需额外权限）

查询当前登录用户所拥有的权限集。

```json
{
  "type": "permission.self",
  "role": "admin",
  "from": "admin_001",
  "msgId": "perm-s-1",
  "data": {}
}
```

**成功响应**：

```json
{
  "type": "permission.self.response",
  "code": 0,
  "msg": "ok",
  "data": {
    "permissions": ["person.create", "person.read", "person.update", "..."]
  }
}
```

---

## 15. 设备指令转发

**所需权限**：`device.command`

管理端可以向在线设备发送指令，由服务端路由转发。

**请求**：

```json
{
  "type": "device.command",
  "role": "admin",
  "from": "admin_001",
  "to": "DEV001",
  "msgId": "cmd-1",
  "data": {
    "command": "reboot",
    "params": {}
  }
}
```

**处理流程**：

1. 服务端检查 `device.command` 权限
2. 查找目标设备是否在线
3. 在线：转发指令到设备，等待 `device.command.ack` 后回写管理端
4. 离线：返回错误码 `5001`（设备不在线）
5. 超时（10 秒）：返回错误码 `5002`（转发超时）

---

## 16. 事件订阅与推送

### 16.1 订阅 `subscribe`

**所需权限**：`event.subscribe`

```json
{
  "type": "subscribe",
  "role": "admin",
  "from": "admin_001",
  "msgId": "sub-1",
  "data": {
    "topics": ["attendance"]
  }
}
```

- `topics` 为字符串数组，单个会话最多订阅 **32** 个主题

**成功响应**：

```json
{
  "type": "subscribe.response",
  "code": 0,
  "msg": "ok",
  "inReplyTo": "sub-1",
  "data": {}
}
```

### 16.2 取消订阅 `unsubscribe`

```json
{
  "type": "unsubscribe",
  "role": "admin",
  "from": "admin_001",
  "msgId": "unsub-1",
  "data": {
    "topics": ["attendance"]
  }
}
```

### 16.3 推送消息

订阅 `attendance` 主题后，当有新的考勤上报时会收到推送：

```json
{
  "type": "attendance.push",
  "role": "server",
  "from": "server",
  "ts": 1715420000123,
  "data": {
    "employeeId": "EMP001",
    "checkTime": "2025-05-10 09:00:00",
    "deviceId": "DEV001",
    "status": "normal"
  }
}
```

---

## 17. 错误码参考


| 错误码    | 常量名                       | 含义        | 触发场景                          |
| ------ | ------------------------- | --------- | ----------------------------- |
| `0`    | —                         | 成功        | 操作正常完成                        |
| `1001` | `kCodeParseError`         | 解析错误      | JSON 格式错误                     |
| `1002` | `kCodePayloadTooLarge`    | 载荷过大      | 超出 JSON 行/二进制帧大小限制            |
| `2001` | `kCodeNotAuthenticated`   | 未认证       | 未登录即发送业务消息                    |
| `2002` | `kCodeAuthFailed`         | 认证失败      | 用户名/密码错误或账号锁定                 |
| `2003` | `kCodeDuplicateSession`   | 重复会话      | 同设备重复登录（视策略而定）                |
| `3001` | `kCodeForbidden`          | 权限不足      | 缺少所需权限或角色                     |
| `4000` | `kCodeBusinessValidation` | 业务校验失败    | 缺少必填字段、字段格式错误等                |
| `4001` | —                         | 人脸检测失败    | 照片中未检测到人脸（人脸注册专用）             |
| `4002` | —                         | 多人脸检测     | 照片中检测到多张人脸（人脸注册专用）            |
| `4003` | —                         | 人脸尺寸不足    | 人脸区域过小（人脸注册专用）               |
| `4004` | —                         | 特征提取失败    | 图片质量不足以提取特征（人脸注册专用）           |
| `4005` | —                         | 图片格式错误    | 不支持的图片格式或图片损坏（人脸注册专用）         |
| `4006` | —                         | 员工不存在     | 指定的 employeeId 在系统中不存在（人脸注册专用） |
| `4007` | —                         | 人脸已存在     | 员工已有数据且未设置覆盖（人脸注册专用）          |
| `5001` | `kCodeDeviceOffline`      | 设备离线      | 设备指令目标不在线                     |
| `5002` | `kCodeForwardTimeout`     | 转发超时      | 设备 10 秒内未响应指令                 |
| `6001` | `kCodeDuplicateKey`       | 唯一键冲突     | 插入重复的 employeeId、deviceId 等   |
| `6002` | `kCodeDbError`            | 数据库错误     | MySQL 操作异常                    |


---

## 18. RBAC 权限参考

### 18.1 预置角色


| 角色 Key               | 名称    | 权限范围                             |
| -------------------- | ----- | -------------------------------- |
| `super_admin`        | 超级管理员 | 全部 23 项权限                        |
| `hr_admin`           | 人事管理员 | 员工 CRUD + 考勤 CRUD + 归档 + 人脸 + 订阅 |
| `device_admin`       | 设备管理员 | 设备 CRUD + 设备指令 + 订阅              |
| `attendance_auditor` | 考勤审核员 | 员工查询 + 考勤读/改 + 归档查询 + 订阅         |
| `viewer`             | 只读观察者 | 员工/考勤/归档/设备/人脸的只读查询              |


### 18.2 全部权限清单（23 项）


| 资源             | 权限 Key                      | 说明         |
| -------------- | --------------------------- | ---------- |
| **person**     | `person.create`             | 创建人员       |
|                | `person.read`               | 查询人员       |
|                | `person.update`             | 修改人员       |
|                | `person.delete`             | 删除人员       |
| **attendance** | `attendance.create`         | 创建考勤       |
|                | `attendance.read`           | 查询考勤       |
|                | `attendance.update`         | 修改考勤       |
|                | `attendance.delete`         | 删除考勤       |
|                | `attendance.archive.read`   | 查询归档       |
|                | `attendance.archive.delete` | 删除归档       |
| **device**     | `device.create`             | 创建设备       |
|                | `device.read`               | 查询设备       |
|                | `device.update`             | 修改设备       |
|                | `device.delete`             | 删除设备       |
|                | `device.command`            | 发送设备指令     |
| **user**       | `user.create`               | 创建用户       |
|                | `user.read`                 | 查询用户/角色/权限 |
|                | `user.update`               | 修改用户       |
|                | `user.delete`               | 删除用户       |
| **face**       | `face.read`                 | 查询人脸       |
|                | `face.delete`               | 删除人脸       |
|                | `face.register`             | 注册人脸特征     |
| **event**      | `event.subscribe`           | 订阅事件       |


### 18.3 权限检查机制

- **资源 CRUD 操作**：检查 `ConnContext.permissions` 中是否包含对应的权限 key
- **角色管理操作**（角色 CRUD、用户角色分配/撤销）：检查 `ConnContext.roles` 中是否包含 `super_admin`
- `**permission.self`**：仅检查是否为已认证的管理端身份

---

## 19. 开发最佳实践

### 19.1 连接管理

1. **使用长连接**：建立 TCP 连接后保持连接，通过心跳维持
2. **自动重连**：检测到连接断开后自动重连并重新认证
3. **消息 ID**：为每条请求生成全局唯一的 `msgId`（推荐 UUID），通过 `inReplyTo` 匹配响应

### 19.2 请求-响应匹配

```
管理端                          服务端
  |--- person.query (msgId: "abc") --->|
  |                                     |
  |<-- person.query.response            |
  |    (inReplyTo: "abc") -------------|
```

建议在客户端维护一个 `map<msgId, callback>` 映射表，收到响应时通过 `inReplyTo` 查找并调用对应的回调。

### 19.3 错误处理

```
if (response.code !== 0) {
    switch (response.code) {
        case 3001:  // 权限不足
            // 提示用户联系管理员
            break;
        case 4000:  // 参数校验失败
            // 显示 response.msg 中的具体错误信息
            break;
        case 6001:  // 唯一键冲突
            // 提示重复数据
            break;
        default:
            // 通用错误处理
    }
}
```

### 19.4 权限适配 UI

登录成功后利用 `data.permissions` 数组动态控制 UI 元素：

```javascript
const permissions = authResponse.data.permissions;

// 根据权限显示/隐藏菜单
showPersonMenu    = permissions.includes("person.read");
showDeviceMenu    = permissions.includes("device.read");
enableCreateBtn   = permissions.includes("person.create");

// 角色管理仅对 super_admin 可见
const roles = authResponse.data.roles;
showRbacMenu = roles.includes("super_admin");
```

也可以随时通过 `permission.self` 请求刷新权限（权限可能因角色变更被热更新）。

### 19.5 数据字段命名约定

所有 JSON 字段使用 **camelCase** 风格：


| 数据库列名             | JSON 字段名        |
| ----------------- | --------------- |
| `employee_id`     | `employeeId`    |
| `check_time`      | `checkTime`     |
| `device_id`       | `deviceId`      |
| `device_name`     | `deviceName`    |
| `ip_address`      | `ipAddress`     |
| `last_online`     | `lastOnline`    |
| `created_at`      | `createdAt`     |
| `updated_at`      | `updatedAt`     |
| `received_time`   | `receivedTime`  |
| `last_login_time` | `lastLoginTime` |
| `feature_size`    | `featureSize`   |
| `role_key`        | `roleKey`       |
| `role_name`       | `roleName`      |
| `perm_key`        | `permKey`       |
| `perm_name`       | `permName`      |
| `is_system`       | `isSystem`      |


### 19.6 消息类型命名规则


| 规则                            | 示例                                 |
| ----------------------------- | ---------------------------------- |
| 请求：`resource.action`          | `person.create`、`attendance.query` |
| 响应：`resource.action.response` | `person.create.response`           |
| 复合资源：`resource.sub.action`    | `attendance.archive.query`         |
| RBAC 操作：`entity.action`       | `role.create`、`user.role.assign`   |


### 19.7 典型开发流程示例

以下是一个完整的管理端会话示例：

```
1. TCP 连接到服务端
2. 发送 auth → 收到 auth_response（获取 roles + permissions）
3. 启动心跳定时器
4. 根据 permissions 渲染 UI
5. 用户操作：
   - 查询人员列表 → person.query
   - 新增人员     → person.create
   - 查询考勤     → attendance.query
   - 修改考勤状态 → attendance.update
6. 订阅考勤推送 → subscribe
7. 实时接收 attendance.push 推送
8. 断开连接前取消订阅 → unsubscribe
```

### 19.8 调试建议

- 所有写操作（create/update/delete/assign/revoke）会自动写入 `OperationLog` 审计日志，可通过数据库直接查看操作记录
- 服务端控制台会输出协议错误、数据库异常等日志
- 使用 `permission.self` 验证当前权限是否符合预期
- 发送空 `data: {}` 的查询请求可获取全量数据（如 `person.query`、`device.query`）

---

*文档生成依据：attendanceServer 源码（`protocol/`、`service/AdminCrudService.cpp`、`protocol/GatewaySessionHandler.cpp`）；如代码变更请同步更新。*