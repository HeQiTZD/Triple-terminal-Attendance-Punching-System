# 管理端（Attendance Admin Server）开发文档

## 一、项目概述

### 1.1 项目定位
本项目为局域网智能考勤系统的管理端，部署在管理员办公电脑，作为 TCP 服务端，负责人员数据管理、人脸数据维护、打卡记录接收/统计/导出、打卡端设备管控。

### 1.2 核心架构
- **架构模式**：C/S 局域网架构中的服务端
- **核心职责**：数据唯一源头、管控中心
- **技术栈**：Qt 5.15/6.2+、C++11+、CMake、MySQL
- **Qt 依赖模块**：Core、Gui、QML、Quick、Network、Sql

## 二、核心功能

1. **人员数据管理**：增删改查人员信息
2. **人脸数据维护**：录入、更新、删除人脸数据
3. **打卡记录处理**：接收、存储、查询打卡记录
4. **数据统计分析**：生成考勤统计报表和图表
5. **数据导出**：支持 Excel/CSV 格式导出
6. **设备管控**：监控打卡端在线状态，下发配置和人员数据

## 三、模块结构

- **TcpServer**：TCP 服务端模块，管理多客户端连接
- **DataManager**：数据管理核心模块
- **FaceDataManager**：人脸数据管理模块
- **AttendanceAnalyzer**：考勤数据分析模块
- **ExportManager**：数据导出模块
- **DeviceMonitor**：设备监控模块
- **QmlUI**：QML用户界面模块

## 四、流程设计

1. 启动管理端，初始化 TCP 服务端
2. 等待打卡端连接
3. 接收打卡端上传的打卡记录
4. 处理人员数据管理操作，下发到打卡端
5. 生成统计报表和导出数据

## 五、数据库设计

### 5.1 人员表（Person）
- id：主键
- name：姓名
- employee_id：工号
- department：部门
- position：职位
- face_feature：人脸特征数据
- created_at：创建时间
- updated_at：更新时间

### 5.2 打卡记录表（AttendanceRecord）
- id：主键
- person_id：人员ID
- check_time：打卡时间
- device_id：打卡设备ID
- status：打卡状态
- received_time：接收时间

### 5.3 设备表（Device）
- id：主键
- device_id：设备标识
- device_name：设备名称
- ip_address：IP地址
- last_online：最后在线时间
- status：在线状态

## 六、网络通信设计

### 6.1 TCP 连接管理
- 使用 QTcpServer 监听指定端口，支持多客户端连接
- 连接状态检测：定时心跳机制，确保连接稳定性
- 维护客户端状态，支持设备在线监控

### 6.2 数据传输协议
- **数据格式**：JSON 格式，便于解析和扩展
- **消息类型**：
  - 人员数据同步
  - 打卡记录上传
  - 设备状态上报
  - 配置下发

### 6.3 客户端管理
- 支持多打卡端同时接入
- 检测到客户端断开后，更新设备状态
- 客户端重新连接后，自动同步数据

## 七、技术实现要点

### 7.1 多客户端管理
- 使用多线程或事件循环处理多个客户端连接
- 维护客户端状态，支持设备在线监控
- 实现客户端认证和权限管理

### 7.2 数据统计分析
- 设计高效的统计算法，生成考勤报表
- 支持按部门、时间段、人员等维度进行统计
- 提供可视化图表展示

### 7.3 数据导出功能
- 支持 Excel/CSV 格式导出
- 确保导出数据的完整性和准确性
- 优化导出性能，支持大量数据导出

## 八、部署方案

- **安装位置**：管理员办公电脑
- **配置要求**：
  - 配置监听端口
  - 确保局域网内打卡端可以访问
  - 安装必要的依赖库

## 九、测试方案

- **功能测试**：测试人员管理、数据接收、统计分析等核心功能
- **网络测试**：测试多打卡端同时接入的性能
- **稳定性测试**：长时间运行测试，确保系统稳定
- **安全性测试**：测试数据传输的安全性

## 十、项目结构

```
AttendanceAdminServer/
├── CMakeLists.txt
├── src/
│   ├── TcpServer/           # TCP服务端模块
│   ├── DataManager/         # 数据管理模块
│   ├── FaceDataManager/     # 人脸数据管理模块
│   ├── AttendanceAnalyzer/  # 考勤分析模块
│   ├── ExportManager/       # 导出管理模块
│   ├── DeviceMonitor/       # 设备监控模块
│   └── QmlUI/               # QML用户界面模块
└── resources/               # 资源文件
```
