# AttendanceSystem（打卡端）项目详细说明

## 1. 项目概览

该项目为局域网考勤系统的**打卡端（客户端）**，核心职责：

- **采集视频帧**（Qt Multimedia）
- **人脸检测 + 特征提取 + 特征比对**（ArcFace 引擎封装）
- **本地落库**（SQLite：人员库、打卡记录、上传标记）
- **TCP 长连接通信**（Qt Network：认证、心跳、人员同步、打卡上传、设备状态上报）
- **断网缓存**（内存队列缓存待发送消息，网络恢复批量补发）

构建入口与依赖见 `CMakeLists.txt`。

## 2. 目录与模块划分

### 2.1 顶层入口

- `main.cpp`：应用样式/调色板/QSS 加载、配置目录初始化、创建 `MainWindow`
- `mainwindow.h/.cpp`：主窗口与**模块集成点**（线程、信号槽、UI 更新、业务编排）

### 2.2 核心模块

- **UI**
  - `UI/facevideowidget.h/.cpp`：自定义视频显示控件，绘制视频帧 + 人脸框
  - `UI/setwindow.h/.cpp/.ui`：设置窗口（读取/修改配置项）
- **CameraCapture**
  - `CameraCapture/cameracapture.*`：摄像头初始化/选择（QCamera）
  - `CameraCapture/videoframecapture.*`：视频帧接收（QMediaCaptureSession + QVideoSink），输出 `QImage` 帧流
  - `CameraCapture/videoframeconverter.*`：`QVideoFrame -> QImage` 转换
- **FaceRecognition**
  - `FaceRecognition/arcfaceengine.*`：ArcFace SDK 二次封装（初始化、detect、extract）
  - `FaceRecognition/facedatabasemanager.*`：特征库（从数据库加载到内存、findBestMatch）
  - `FaceRecognition/facerecognizer.*`：业务状态机（检测/识别/冷却/丢失），输出 UI 信号与落库请求
- **LocalStorage**
  - `LocalStorage/localstorage.*`：SQLite 初始化、人员同步事务、打卡记录写入、未上传记录查询/标记
- **NetworkClient**
  - `NetworkClient/connectionmanager.*`：TCP 连接、断线重连（指数退避）、状态信号
  - `NetworkClient/messagereader.*`：`\n` 分隔的 JSON 流读取与解析（处理粘包）
  - `NetworkClient/messagewriter.*`：JSON 序列化 + 追加 `\n` 后发送；支持批量发送
  - `NetworkClient/messagequeue.*`：线程安全队列（断网/发送失败缓存）
  - `NetworkClient/heartbeatmanager.*`：心跳定时发送 + 超时检测（等待响应）
  - `NetworkClient/serverprotocol.*`：**与 AttendanceServer 对齐的协议键名/消息类型**、构建与解析
  - `NetworkClient/networkclient.*`：网络模块门面（认证/心跳/同步/上传/上报），对外仅暴露业务接口与状态信号
  - `NetworkClient/protocol.*`：历史/通用协议结构（当前主链路已主要使用 `ServerProtocol`，仍保留 `Protocol::PersonData` 等数据类型）
- **Config**
  - `Config/configmanager.*`：配置读写（服务端地址、数据库路径、日志路径、ArcFace appid/sdkKey、阈值等）

## 3. 关键类与职责（Class Map）

### 3.1 `MainWindow`

文件：`mainwindow.h/.cpp`

- **职责**
  - 创建并初始化：数据库、摄像头、帧采集、人脸识别、网络客户端
  - 线程隔离：网络模块线程、人脸识别线程
  - 业务编排：识别成功 -> UI 更新；识别请求保存 -> 主线程落库 -> 网络上传
  - 网络状态 UI：监听 `Networkclient::networkStateChanged`

- **关键成员**
  - `LocalStorage* m_db`
  - `FaceRecognizer* m_FaceRecognizer`
  - `Networkclient* networkClient`
  - `CameraCapture* m_CameraCapture`
  - `VideoFrameCapture* m_VideoFrameCapture`
  - `QThread* m_faceThread`, `QThread* m_networkThread`

### 3.2 `VideoFrameCapture`

文件：`CameraCapture/videoframecapture.h/.cpp`

- **职责**
  - 通过 `QMediaCaptureSession` + `QVideoSink` 获取帧
  - 将帧转换为 `QImage`，并通过信号 `frameCaptured(QImage)` 向外发布
  - 提供 `FaceVideoWidget` 作为预览控件：`getVideoWidget()`

### 3.3 `FaceVideoWidget` 与 `FaceRectInfo`

文件：`UI/facevideowidget.h/.cpp`

- **职责**
  - `setCurrentFrame()`：接收当前帧（线程安全存储）并触发重绘
  - `setFaceRects()`：接收人脸框列表并绘制（绿色=已识别，红色=未识别）
  - `paintEvent()`：按保持宽高比的 `targetRect` 绘制视频帧，并将人脸坐标映射到控件坐标系

- **数据类型**
  - `struct FaceRectInfo { QRect rect; QString name; bool recognized; };`

### 3.4 `FaceRecognizer`（人脸识别业务状态机）

文件：`FaceRecognition/facerecognizer.h/.cpp`

- **职责**
  - `WanZhengYeWuLiuCheng(QImage)`：主入口（由视频帧驱动）
  - 状态机：`IDLE -> DETECTING -> RECOGNIZED -> LOST -> IDLE`
  - 识别流程：detectFace -> extractFeature -> findBestMatch -> threshold -> 输出信号
  - 去重：冷却窗口 `COOLDOWN_MS` 内同一 `employeeId` 不重复落库

- **对外信号**
  - `recognitionSuccess(employeeId, name, status, checkTime, faceImage)`
  - `requestSaveAttendance(employeeId, status)`：**让主线程执行数据库写入**
  - `faceDetected(faceInfos)`：UI 绘制人脸框

### 3.5 `LocalStorage`（SQLite 本地存储）

文件：`LocalStorage/localstorage.h/.cpp`

- **职责**
  - 初始化数据库与表结构：`Person`、`AttendanceRecord`
  - `syncPersons()`：事务方式同步人员（当前实现为“清空再插入”）
  - `addAttendanceRecord()`：插入打卡记录（默认 `uploaded=0`）
  - `getUnuploadedRecords()` / `markAsUploaded()` / `markBatchAsUploaded()`

- **数据表（实际建表 SQL 摘要）**
  - `Person(employee_id TEXT PRIMARY KEY, name TEXT, face_feature BLOB, face_feature_size INTEGER, last_updated DATETIME)`
  - `AttendanceRecord(id INTEGER PK AUTOINCREMENT, employee_id TEXT FK, check_time DATETIME, status TEXT, uploaded INTEGER, upload_time DATETIME)`

### 3.6 `Networkclient`（网络门面）与子模块

文件：`NetworkClient/networkclient.h/.cpp`

- **职责**
  - 对外业务接口：
    - `connectToServer(ip, port)`
    - `syncPersonData()`
    - `uploadAttendance(employeeId, status, checkTime)`
    - `uploadAttendanceBatch(records)`
    - `reportDeviceStatus(statusPayload)`
  - 内部创建与编排：
    - `Connectionmanager`：负责连接/重连
    - `Messagewriter`：负责发送（JSON + `\n`）
    - `Messagereader`：负责接收与解析（按 `\n` 切分）
    - `Heartbeatmanager`：定时发送心跳，等待服务器响应，超时触发断开以重连
    - `Messagequeue`：断网/发送失败缓存，网络恢复批量补发
  - **认证优先**：连接成功后立即发送 `auth`，收到 `auth_response` 成功后触发 `sync_request`

## 4. 典型调用链（Call Chain）

### 4.1 启动链路（App Boot）

1. `main.cpp`：
   - 初始化 Qt `QApplication`
   - 加载 `:/qss/mainwindow.qss`
   - `ConfigManager::instance()` 初始化并确保目录存在
2. 创建 `MainWindow`：
   - `restoreWindowSize()`
   - `initNetworkClient()`：获取 `Networkclient::instance()` 并 `moveToThread(m_networkThread)`
   - `initNetWorkStatus()`：跨线程连接 `Networkclient::networkStateChanged`
   - `startNetworkConnection()`：从配置读取 `serverIp/serverPort`，`invokeMethod` 异步发起连接
   - `init()`：初始化 `LocalStorage`、`FaceRecognizer`、摄像头、`VideoFrameCapture`
   - `FaceFeatureStart()`：连接“帧流 -> 人脸识别 -> UI/落库/画框”

### 4.2 识别与落库/上传链路（核心业务闭环）

1. `VideoFrameCapture::processFrame()` 发射 `frameCaptured(QImage)`
2. `MainWindow` 连接：
   - `frameCaptured -> FaceRecognizer::WanZhengYeWuLiuCheng`（识别线程）
   - 同时 `frameCaptured -> FaceVideoWidget::setCurrentFrame`（UI 线程更新画面）
3. `FaceRecognizer`：
   - `detectFace` -> `emit faceDetected(faceInfos)`（用于 UI 框）
   - `extractFeature` -> `FaceDatabaseManager::findBestMatch` -> 阈值判断
   - 首次识别到新人员：`emit requestSaveAttendance(employeeId, status)`
   - 同时 `emit recognitionSuccess(...)` 更新 UI 展示
4. `MainWindow::onSaveAttendanceRequest()`（主线程）：
   - `LocalStorage::addAttendanceRecord(employeeId, status)`
   - 成功后：`Networkclient::uploadAttendance(employeeId, status, now)`
5. `Networkclient::uploadAttendance()`：
   - 在线：`Messagewriter::send(attendance_record)`；失败则入队
   - 离线：直接入队（业务层视为成功）
6. 网络恢复/重连成功后 `Networkclient::processQueue()` 批量补发

### 4.3 网络连接、认证、同步、心跳链路

1. `Connectionmanager::connectToHost(ip,port)` 建立 TCP
2. 连接成功：
   - `Networkclient::onConnectionConnected()`：
     - 从 socket 创建 `Messagewriter` 与 `Messagereader`
     - `Messagereader::start()`
     - 发送认证：`ServerProtocol::buildAuth(deviceId)`
     - `Heartbeatmanager::setSocket(socket)` + `start(3000)`
     - `processQueue()` 发送断网缓存
3. 接收消息：
   - `Messagereader` 解析成功发射 `messageReceived(QJsonObject)`
   - `Networkclient::onMessageReceived()` 用 `ServerProtocol::parseType()` 分发：
     - `auth_response`：认证成功后 `sync_request`
     - `heartbeat_response`：调用 `Heartbeatmanager::onHeartbeatResponse()`
     - `person_sync`：解析人员 -> `LocalStorage::syncPersons()` -> `FaceDatabaseManager::loadFromDatabase()`
     - `error`：上抛为 `uploadFinished(false, message)`
4. 断开/错误：
   - `Connectionmanager` 断开后按指数退避重连（最多 5 次）
   - 心跳超时会触发 `Networkclient::onHeartbeatTimeout()`，主动断开让 `Connectionmanager` 重连

## 5. 通信协议（AttendanceServer 对齐）

### 5.1 传输层与分帧

- **传输**：TCP 长连接（`QTcpSocket`）
- **消息边界**：使用换行符 `\n` 作为分隔（每条 JSON 一行）
  - 发送侧：`Messagewriter` 在每条 JSON 后 `append("\n")`
  - 接收侧：`Messagereader` 缓冲区中按 `\n` 查找并切割，逐条 `QJsonDocument::fromJson`

### 5.2 消息公共字段（`NetworkClient/serverprotocol.h`）

根对象关键键：

- `type`：消息类型（字符串）
- `deviceId`：设备编号（客户端侧 `Networkclient::m_deviceId`）
- `status`：状态字段（不同消息含义不同：如认证结果、设备在线状态等）
- `message`：错误说明（`type=error` 时）

### 5.3 已实现的消息类型（type）

在 `ServerProtocol` 中定义（必须与服务器一致）：

- `auth` / `auth_response`
- `heartbeat` / `heartbeat_response`
- `sync_request`
- `person_sync`
- `attendance_record`
- `device_status`
- `error`

### 5.4 消息结构与示例

#### 5.4.1 认证

- 请求（客户端 -> 服务器）：

```json
{"type":"auth","deviceId":"device_001"}
```

- 响应（服务器 -> 客户端，示例）：

```json
{"type":"auth_response","status":"success"}
```

#### 5.4.2 心跳

- 请求：

```json
{"type":"heartbeat"}
```

- 响应（示例）：

```json
{"type":"heartbeat_response","status":"ok"}
```

> 客户端在收到 `heartbeat_response` 后调用 `Heartbeatmanager::onHeartbeatResponse()` 解除等待；否则超时触发重连。

#### 5.4.3 同步人员请求

- 请求：

```json
{"type":"sync_request","deviceId":"device_001"}
```

#### 5.4.4 人员同步下发（服务器 -> 客户端）

- 响应：

```json
{
  "type":"person_sync",
  "persons":[
    {"id":1,"name":"张三","employeeId":"E0001","department":"研发","position":"工程师"}
  ]
}
```

客户端侧解析：`ServerProtocol::parsePersons()` -> 转换为 `Protocol::PersonData` 并调用 `LocalStorage::syncPersons()`。

#### 5.4.5 打卡记录上传（客户端 -> 服务器）

```json
{
  "type":"attendance_record",
  "employeeId":"E0001",
  "checkTime":"2026-05-04T17:21:00",
  "deviceId":"device_001",
  "status":"正常"
}
```

字段说明：

- `employeeId`：工号
- `checkTime`：ISO8601（客户端使用 `QDateTime::toString(Qt::ISODate)`）
- `status`：打卡状态（当前业务里多用“正常”等中文值；服务器侧需兼容）

#### 5.4.6 设备状态上报（客户端 -> 服务器）

```json
{
  "type":"device_status",
  "deviceId":"device_001",
  "status":"online",
  "deviceName":"front-desk-01",
  "ipAddress":"192.168.1.10"
}
```

> `Networkclient::reportDeviceStatus()` 会把调用方传入的 payload 作为根对象，并强制补齐 `type/deviceId/status`。

#### 5.4.7 错误消息（服务器 -> 客户端）

```json
{"type":"error","message":"unauthorized"}
```

客户端侧：`Networkclient::handleServerError()` -> `uploadFinished(false, message)`。

## 6. 数据类型（Data Types）

### 6.1 协议/网络数据类型

- `ServerProtocol::MessageType`（仅用于客户端内部分类）
  - `AuthResponse / HeartbeatResponse / PersonSync / Error / Unknown`
- `ServerProtocol::PersonSyncItem`
  - `int id`
  - `QString name`
  - `QString employeeId`
  - `QString department`
  - `QString position`

### 6.2 本地业务数据类型（`NetworkClient/protocol.h`）

> 该文件提供了结构化数据与 JSON 序列化方法，当前网络主通道已以 `ServerProtocol` 为准，但仍复用其中数据结构（如 `PersonData`）。

- `Protocol::AttendanceRecord`
  - `QString employeeId`
  - `QString checktTime`
  - `QString status`
  - `QJsonObject toJson() / static fromJson()`
- `Protocol::PersonData`
  - `QString employeeId`
  - `QString name`
  - `QByteArray faceFeature`（若走 JSON 需 base64）
  - `int featureSize`

### 6.3 人脸与 UI 数据类型

- `arcfaceengine::FaceInfo`
  - `QRect rect`（检测框）
  - 其他字段由 ArcFace 封装决定
- `arcfaceengine::FaceFeature`
  - `QByteArray data`（特征向量）
- `FaceRectInfo`（用于绘制）
  - `QRect rect`
  - `QString name`
  - `bool recognized`

## 7. 线程与并发模型

- **UI/Main 线程**
  - `MainWindow`、`FaceVideoWidget` 绘制、SQLite 操作（`LocalStorage`）在主线程调用
  - `FaceRecognizer::requestSaveAttendance` 用 `Qt::QueuedConnection` 投递到主线程执行
- **网络线程**
  - `Networkclient` 被移动到 `m_networkThread`
  - 连接发起通过 `QMetaObject::invokeMethod(..., Qt::QueuedConnection)` 保证跨线程安全
- **人脸识别线程**
  - `FaceRecognizer` 移动到 `m_faceThread`
  - 帧信号连接到 `WanZhengYeWuLiuCheng` 触发识别

## 8. 可扩展点与注意事项

- **协议一致性**
  - 当前“服务器对齐协议”以 `ServerProtocol` 为准（type/keys/结构）
  - `Protocol.*` 中存在字段名差异（例如 `employeeID` vs `employeeId`、`checktTime` 拼写），如需统一建议后续收敛
- **断网缓存持久化**
  - 当前缓存为内存队列 `Messagequeue`；若要进程重启后不丢，需要将待发送消息落到 SQLite（或文件）并在启动时恢复
- **人员同步数据**
  - `person_sync` 当前未包含人脸特征；客户端会把 `faceFeature` 留空并仍执行 `syncPersons()`（数据库约束 `face_feature BLOB NOT NULL` 需与实际数据匹配，后续若服务器下发特征应补齐）
- **上传成功回执**
  - 代码里已有 `handleUploadResponse()` 但当前 `ServerProtocol::parseType()` 未覆盖 `upload_response` 类型；若服务器提供回执，建议补充类型与处理逻辑并驱动 `LocalStorage::markAsUploaded(...)`

