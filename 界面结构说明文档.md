# AttendanceAdmin 界面结构说明文档

## 1. 项目概述

AttendanceAdmin 是一个基于 Qt 6.8+ / C++17 的 Windows 桌面考勤管理系统客户端。采用 QML 构建 UI 界面，通过自定义 JSON-over-TCP 协议与考勤设备服务端通信。

### 1.1 技术栈
- **UI 框架**: Qt Quick (QML)
- **开发语言**: QML + C++17
- **构建系统**: CMake + Ninja
- **编译器**: MSVC 2022
- **Qt 版本**: 6.10.2
- **渲染后端**: D3D11 (QSG_RHI_BACKEND=d3d11)
- **控件样式**: Fusion (QT_QUICK_CONTROLS_STYLE=Fusion)

### 1.2 窗口特性
- **无边框窗口**: 使用 `Qt.FramelessWindowHint`
- **固定尺寸**: 主窗口 1480×920，登录窗口 400×542
- **强制浅色模式**: `Qt::ColorScheme::Light`

---

## 2. 整体界面架构

### 2.1 顶层结构

```
ApplicationWindow (Main.qml)
├── StackLayout
│   ├── LoginPage (未登录状态)
│   └── SplitView (已登录状态)
│       ├── SideBar (左侧导航栏, 220px)
│       └── ColumnLayout (右侧内容区)
│           ├── StatusBar (顶部状态栏, 36px)
│           └── PageHost (页面容器, 动态加载)
```

### 2.2 页面路由机制

**PageHost.qml** 作为页面容器，根据 `pageKey` 动态加载对应页面组件：

| pageKey | 页面组件 | 功能描述 |
|---------|----------|----------|
| `dashboard` | PageDashboard | 仪表盘，显示统计概览 |
| `person` | PagePerson | 人员管理 |
| `device` | PageDevice | 设备管理 |
| `configDeploy` | PageConfigDeploy | 配置下发 |
| `attendance` | PageAttendance | 考勤记录管理 |
| `face` | PageFace | 人脸数据管理 |
| `user` | PageUser | 用户账号管理 |
| `rbac` | PageRbac | 角色权限管理 |
| `logViewer` | PageLogViewer | 日志查看 |

---

## 3. 导航系统

### 3.1 侧边栏 (SideBar.qml)

**布局结构**:
```
SideBar (220px宽, #E8EBF1背景)
├── 标题栏 (56px高)
│   └── Label: "考勤管理系统"
├── 分隔线
├── 导航列表 (ListView)
│   ├── 分组标题 (26px高, 全大写灰色文字)
│   └── 导航项 (38px高)
│       ├── 左侧指示条 (3px宽, 选中时显示蓝色)
│       ├── 图标 (Unicode字符)
│       └── 文字标签
├── 分隔线
├── 退出登录按钮
└── 版本号 (v0.1 · Qt 6)
```

**交互状态**:
- **选中态**: 浅蓝色背景 (`#C7DFFF`) + 蓝色左边条 + 加粗文字
- **悬停态**: 灰色背景 (`#CBD5E1`)
- **普通态**: 透明背景

### 3.2 状态栏 (StatusBar.qml)

**布局结构**:
```
StatusBar (36px高, 白色背景)
├── 服务器状态徽章 (BadgeStatus)
│   ├── 绿色圆点 + "服务器已连接"
│   └── 红色圆点 + "服务器未连接"
├── 用户信息标签
│   └── "用户名 · 角色1、角色2"
├── 弹性空间
└── 窗口控制按钮组 (WindowCaptionButtons)
    ├── 最小化
    ├── 最大化/还原
    └── 关闭
```

---

## 4. 页面结构模板

### 4.1 标准页面布局

所有业务页面遵循统一的布局模式：

```
Page (Item)
├── ColumnLayout (fill, margins: 16px)
│   ├── ToolBarRow (页面标题栏, 56px高)
│   │   ├── 左侧: 标题 + 副标题
│   │   └── 右侧: 操作按钮槽
│   ├── Card (筛选/表单区域)
│   │   ├── 标题栏 (38px高)
│   │   │   ├── 标题文字
│   │   │   └── 右侧操作按钮
│   │   ├── 分隔线
│   │   └── 内容区
│   │       └── GridLayout / RowLayout (表单字段)
│   └── Card (数据列表区域, 填充高度)
│       ├── 标题栏
│       ├── 分隔线
│       └── DataTable (数据表格)
├── BusyOverlay (加载遮罩, z:100)
├── ConfirmDialog (确认对话框)
└── Popup/Toast (消息提示)
```

### 4.2 各页面详细结构

#### 4.2.1 仪表盘 (PageDashboard)

```
PageDashboard
├── ToolBarRow: "欢迎，用户名"
├── GridLayout (3列, 统计卡片)
│   ├── StatCard: 员工人数
│   ├── StatCard: 今日打卡人数
│   └── StatCard: 设备连接数量
└── RowLayout (趋势区域)
    ├── Card (55%宽度): "最近打卡记录"
    │   └── RecentPunchList
    └── Card (45%宽度): "今日打卡类型"
        └── AttendanceStatusPieChart
```

#### 4.2.2 人员管理 (PagePerson)

```
PagePerson
├── ToolBarRow: "人员管理"
├── Card: "人员信息"
│   └── GridLayout (4列)
│       ├── LabeledField: 姓名
│       ├── LabeledField: 工号
│       ├── LabeledField: 部门
│       ├── LabeledField: 岗位
│       └── Row (操作按钮)
│           ├── PermissionButton: 新增 (highlighted)
│           ├── PermissionButton: 修改
│           ├── PermissionButton: 删除
│           ├── PermissionButton: 查询
│           └── PermissionButton: 清空表单 (flat)
├── Card: "人员列表"
│   ├── subtitle: "点击行加载到表单" / "只读列表"
│   └── DataTable
└── ConfirmPopup + ToastPopup
```

#### 4.2.3 设备管理 (PageDevice)

```
PageDevice
├── ToolBarRow: "设备管理" + [刷新按钮]
├── Card: "设备信息"
│   └── GridLayout (4列)
│       ├── LabeledField: 设备 ID
│       ├── LabeledField: 名称
│       ├── LabeledField: IP
│       ├── LabeledField: 密钥 (Password)
│       ├── LabeledField: 状态 (ComboBox: online/offline/maintenance)
│       └── Row (操作按钮)
├── Card: "设备列表"
│   └── DataTable
└── ConfirmDialog (删除确认)
```

#### 4.2.4 配置下发 (PageConfigDeploy)

```
PageConfigDeploy
├── ToolBarRow: "配置下发" + [刷新设备, 下发配置]
├── ScrollView (可滚动内容区)
│   ├── RowLayout (2列卡片)
│   │   ├── Card: "人脸识别配置"
│   │   │   └── GridLayout (2列)
│   │   │       ├── 阈值 (SpinBox)
│   │   │       ├── 最大人脸数 (SpinBox)
│   │   │       ├── 识别超时 (SpinBox)
│   │   │       ├── AppId (TextField)
│   │   │       └── SdkKey (Password)
│   │   └── Card: "设备信息"
│   │       └── GridLayout (2列)
│   │           ├── 设备 ID
│   │           ├── 设备密钥
│   │           ├── 固件版本
│   │           └── 设备名称
│   ├── Card: "考勤规则配置"
│   │   └── GridLayout (4列)
│   │       ├── 上班/下班时间
│   │       ├── 签到偏移/签退结束
│   │       ├── 迟到/早退容差
│   │       ├── 弹性范围/最小间隔
│   │       ├── 核心开始/结束
│   │       ├── 午休开始/结束
│   │       ├── 最大工时/重试次数
│   │       ├── 退避毫秒
│   │       └── Switches (弹性工作, 午休, 跨天)
│   ├── Card: "目标设备" (520px宽)
│   │   ├── headerRight: [FaceSyncButton, 全选在线, 清空]
│   │   ├── Label: "已选择 N 台"
│   │   └── ListView (设备复选框列表)
│   └── Card: "下发结果" (220px高)
│       └── DataTable
```

#### 4.2.5 考勤记录 (PageAttendance)

```
PageAttendance
├── ToolBarRow: "考勤记录"
├── TabBar
│   ├── Tab: "实时考勤" (attendance.read)
│   └── Tab: "归档记录" (attendance.archive.read)
└── StackLayout
    ├── Tab A: 实时考勤
    │   ├── Card: "筛选 & 新增"
    │   │   └── GridLayout (4列)
    │   │       ├── 员工工号
    │   │       ├── 打卡时间
    │   │       ├── 设备 ID
    │   │       ├── 状态 (ComboBox)
    │   │       └── Row [新增打卡记录, 查询, 使用当前时间]
    │   └── Card: "实时记录"
    │       ├── headerRight: [导出 CSV]
    │       └── DataTable
    └── Tab B: 归档
        ├── Card: "归档筛选"
        │   └── GridLayout (3列) + [查询归档, 删除归档记录]
        └── Card: "归档列表"
            └── DataTable
```

#### 4.2.6 人脸数据管理 (PageFace)

```
PageFace
├── ToolBarRow: "人脸数据管理"
├── RowLayout (操作栏)
│   ├── LabeledField: 工号
│   ├── CheckBox: 覆盖已有
│   ├── 弹性空间
│   ├── PermissionButton: 查询
│   ├── PermissionButton: 注册人脸 (highlighted)
│   └── PermissionButton: 删除
└── Card: "人脸数据"
    └── DataTable
```

#### 4.2.7 用户账号 (PageUser)

```
PageUser
├── ToolBarRow: "用户账号"
├── Card: "账号信息"
│   └── GridLayout (3列)
│       ├── LabeledField: 工号
│       ├── LabeledField: 显示名称
│       ├── LabeledField: 密码 (Password)
│       ├── Label: 密码错误提示 (红色)
│       └── Row [新增, 修改, 删除, 查询, 清空表单]
├── Card: "用户列表"
│   └── DataTable
└── ConfirmDialog
```

#### 4.2.8 角色权限管理 (PageRbac)

```
PageRbac
├── ToolBarRow: "用户权限" / "角色管理"
├── Row (自定义Tab切换)
│   ├── Rectangle: "用户权限分配" (选中时蓝色)
│   └── Rectangle: "角色管理" (选中时蓝色)
└── StackLayout
    ├── Tab 0: 用户权限分配
    │   ├── Card (320px宽): "用户列表"
    │   │   ├── PermissionButton: 刷新
    │   │   └── ListView (用户选择列表)
    │   └── Card: "用户权限"
    │       ├── 用户ID/姓名显示
    │       ├── 当前角色标签
    │       ├── Flow (角色复选框)
    │       └── PermissionButton: 保存角色变更
    └── Tab 1: 角色管理
        ├── Card: "角色列表"
        │   └── DataTable
        ├── Card (空状态): 选择提示
        └── Card: "角色详情" / "新建角色"
            ├── headerRight: [保存/创建/取消/删除]
            ├── GridLayout (3列): 角色标识, 名称, 描述
            └── Flow (权限分组复选框)
                └── 7个权限组 × N个权限项
```

#### 4.2.9 日志查看 (PageLogViewer)

```
PageLogViewer
├── ToolBarRow: "日志查看" + "共 N 条日志"
├── Card: "筛选条件"
│   └── GridLayout (6列)
│       ├── 起始日期 (yyyy-MM-dd)
│       ├── 结束日期
│       ├── 级别 (ComboBox: ALL/INFO/WARN/ERROR)
│       ├── 类型 (ComboBox: ALL/LOGIN/DATA/CONFIG/SYSTEM)
│       ├── 关键词 (搜索框)
│       └── Row [重置, 导出]
└── Card: "日志列表"
    ├── headerRight: 加载状态
    └── 自定义表格 (非DataTable)
        ├── 表头: 时间 | 级别 | 类型 | 消息
        └── 行 (28px高)
            ├── 时间 (等宽字体)
            ├── 级别徽章 (彩色背景)
            ├── 类型徽章
            └── 消息文字
```

---

## 5. 核心组件库

### 5.1 布局组件

#### Card.qml (卡片容器)
```
属性:
  - title: string (标题)
  - subtitle: string (副标题)
  - padding: int (内边距, 默认10px)
  - stretchContent: bool (是否填充高度)
  - elevation: int (阴影等级: 0-3)
  - headerRight: alias (标题栏右侧插槽)

结构:
  Rectangle (白色背景, 6px圆角, 1px边框)
  ├── ColumnLayout
  │   ├── Item (标题栏, 38px高)
  │   │   └── RowLayout
  │   │       ├── ColumnLayout (标题+副标题)
  │   │       └── Row (headerRight插槽)
  │   ├── Rectangle (分隔线, 1px)
  │   └── Item (内容区)
  │       └── Item (contentItem, 可padding)
```

#### ToolBarRow.qml (页面标题栏)
```
属性:
  - title: string
  - subtitle: string
  - actions: alias (操作按钮槽)

结构:
  Rectangle (透明背景, 56px高)
  ├── RowLayout
  │   ├── ColumnLayout (标题+副标题)
  │   └── Row (actions插槽)
  └── Rectangle (底部1px分隔线)
```

#### LabeledField.qml (标签输入框)
```
属性:
  - label: string
  - labelWidth: int (默认80px)
  - labelBold: bool

结构:
  RowLayout
  ├── Label (灰色标签文字, 12px)
  └── [用户传入的控件]
```

### 5.2 数据展示组件

#### DataTable.qml (数据表格)
```
属性:
  - columns: array [{ key, title, width?, align?, formatter? }]
  - rows: array
  - currentRow: int
  - stripeRows: bool (默认true)
  - emptyText: string

功能:
  - 列宽拖拽调整
  - 行选中/高亮
  - 右键菜单 (复制行JSON)
  - 斑马纹

结构:
  Rectangle (背景色, 边框, 4px圆角)
  ├── ColumnLayout
  │   ├── Rectangle (表头, 30px高, #EEF1F6背景)
  │   │   └── Row (列标题)
  │   ├── Rectangle (1px分隔线)
  │   └── ListView (数据行)
  │       └── delegate: Rectangle (28px高)
  │           └── Row (单元格)
  └── TextEdit (隐藏, 用于剪贴板复制)
```

#### BadgeStatus.qml (状态徽章)
```
属性:
  - text: string
  - accent: color (自动根据text映射)
  - dotOnly: bool

结构:
  Rectangle (圆角11px/6px, 半透明背景+边框)
  ├── Row (完整模式)
  │   ├── Rectangle (8px圆点)
  │   └── Label (状态文字)
  └── Rectangle (点模式, 8px圆点)
```

#### StatCard.qml (统计卡片)
```
属性:
  - title: string
  - value: string
  - subtitle: string
  - trend: string (趋势文字)
  - trendPositive: bool
  - icon: string
  - iconColor: color

功能:
  - 悬停变灰
  - 数值变化动画

结构:
  Rectangle (白色背景, 6px圆角, 1px边框, 120px高)
  └── ColumnLayout (margin: 16px)
      ├── RowLayout (图标 + 标题 + 趋势徽章)
      ├── Label (大号数值, 22px, 加粗)
      ├── Label (副标题, 12px, 灰色)
      └── Item (弹性空间)
```

### 5.3 交互组件

#### PermissionButton.qml (权限按钮)
```
属性:
  - sessionManager: var
  - requiredPermission: string
  - requiredRole: string
  - hideWhenDenied: bool (默认true)
  - deniedDialog: var

功能:
  - 自动检查权限
  - 无权限时隐藏或禁用
  - guardedClick() 方法处理权限验证
```

#### ConfirmDialog.qml (确认对话框)
```
属性:
  - message: string

结构:
  Dialog (模态, 居中)
  ├── background: Rectangle (白色, 6px圆角)
  └── contentItem: Label (可换行文字)
```

#### BusyOverlay.qml (加载遮罩)
```
属性:
  - busy: bool

结构:
  Rectangle (半透明灰色背景, z:100)
  ├── MouseArea (阻止交互)
  └── BusyIndicator (旋转加载动画)
```

### 5.4 图表组件

| 组件 | 文件 | 用途 |
|------|------|------|
| AttendanceStatusPieChart | AttendanceStatusPieChart.qml | 考勤状态饼图 |
| BarChart | BarChart.qml | 柱状图 |
| LineChart | LineChart.qml | 折线图 |
| ChartContainer | ChartContainer.qml | 图表容器 |
| ChartLegend | ChartLegend.qml | 图例 |
| RecentPunchList | RecentPunchList.qml | 最近打卡列表 |

---

## 6. 设计系统 (Theme.qml)

### 6.1 颜色体系

#### 主色系
| 名称 | 色值 | 用途 |
|------|------|------|
| primary | `#1D6BFF` | 主色调 |
| primaryHover | `#3B82FF` | 主色悬停态 |
| primarySubtle | `#DBEAFE` | 主色浅色背景 |

#### 背景色板
| 名称 | 色值 | 用途 |
|------|------|------|
| bg | `#F2F4F7` | 页面背景 |
| surface | `#FFFFFF` | 卡片/组件背景 |
| surfaceAlt | `#EEF1F6` | 表头/悬停背景 |
| sideBar | `#E8EBF1` | 侧边栏背景 |
| border | `#D8DDE3` | 边框/分隔线 |
| borderStrong | `#BFC6D2` | 强边框 |

#### 文字色板
| 名称 | 色值 | 用途 |
|------|------|------|
| text | `#1F2937` | 主文字 |
| textMuted | `#3D4551` | 次要文字 |
| textSubtle | `#6B7280` | 辅助文字 |

#### 状态色
| 名称 | 色值 | 用途 |
|------|------|------|
| success | `#0D8A4C` | 成功/在线/正常 |
| warning | `#9A3E04` | 警告/维护 |
| danger | `#C62828` | 错误/失败/离线 |
| info | `#1565C0` | 信息 |

#### 交互色
| 名称 | 色值 | 用途 |
|------|------|------|
| hover | `#CBD5E1` | 悬停背景 |
| selected | `#C7DFFF` | 选中背景 |
| highlight | `#92400E` | 高亮文字 |

### 6.2 间距系统

| 名称 | 值 | 用途 |
|------|-----|------|
| spacingXs | 4px | 最小间距 |
| spacingSm | 6px | 小间距 |
| spacingMd | 10px | 中等间距 |
| spacingLg | 16px | 大间距/页面边距 |
| spacingXl | 24px | 特大间距 |

### 6.3 圆角系统

| 名称 | 值 | 用途 |
|------|-----|------|
| radiusSm | 4px | 小组件/徽章 |
| radiusMd | 6px | 卡片/对话框 |
| radiusLg | 10px | 大组件 |

### 6.4 字号系统

| 名称 | 值 | 用途 |
|------|-----|------|
| fontXs | 11px | 辅助文字 |
| fontSm | 12px | 正文/标签 |
| fontMd | 13px | 主要文字 |
| fontLg | 15px | 标题 |
| fontXl | 18px | 页面标题 |
| fontXxl | 22px | 大号数值 |

### 6.5 字体

| 类型 | 字体族 |
|------|--------|
| 主字体 | Segoe UI, Microsoft YaHei UI, PingFang SC, sans-serif |
| 等宽字体 | Consolas, Cascadia Mono, monospace |

### 6.6 布局尺寸

| 名称 | 值 | 用途 |
|------|-----|------|
| sideBarWidth | 220px | 侧边栏宽度 |
| statusBarHeight | 36px | 状态栏高度 |
| rowHeight | 30px | 表格行高 |
| controlHeight | 30px | 控件高度 |

---

## 7. 权限控制系统

### 7.1 权限标识格式

```
{模块}.{操作}
```

示例：
- `person.read` - 查询人员
- `person.create` - 新增人员
- `device.update` - 修改设备
- `attendance.archive.delete` - 删除归档

### 7.2 权限模块

| 模块 | 权限项 |
|------|--------|
| person | read, create, update, delete |
| device | read, create, update, delete, command |
| attendance | read, create, update, delete |
| attendance.archive | read, delete |
| user | read, create, update, delete |
| face | read, register, delete |
| config | deploy |
| event | subscribe |

### 7.3 特殊角色

- `super_admin` - 超级管理员，可管理角色和权限

### 7.4 权限检查机制

1. **PermissionCatalog** 单例提供权限查询方法
2. **PermissionButton** 组件自动检查权限并控制可见性/启用状态
3. **PageAccessPlaceholder** 在无页面权限时显示占位提示
4. **PermissionDeniedDialog** 权限不足时弹出提示对话框

---

## 8. 数据流架构

### 8.1 服务层

每个业务模块对应一个 C++ 服务类：

| 服务类 | 职责 |
|--------|------|
| PersonServer | 人员 CRUD |
| DeviceServer | 设备 CRUD |
| AttendanceService | 考勤记录查询/新增 |
| ConfigDeployServer | 配置下发 |
| FaceServer | 人脸数据管理 |
| UserServer | 用户账号管理 |
| RbacServer | 角色权限管理 |
| EventSubscriptionService | 事件订阅/推送 |

### 8.2 通信流程

```
QML UI
  ↓ Q_INVOKABLE 方法调用
C++ Service
  ↓ sendMessage(msg, callback)
TcpConnectionManager
  ↓ JSON-over-TCP
Server
  ↓ Response
TcpConnectionManager
  ↓ callback(response)
C++ Service
  ↓ 信号通知
QML UI (属性绑定更新)
```

### 8.3 消息格式

**请求信封**:
```json
{
  "type": "person.query",
  "msgId": "uuid",
  "from": "admin",
  "to": "server",
  "ts": 1234567890,
  "data": { ... }
}
```

**响应信封**:
```json
{
  "type": "person.query",
  "inReplyTo": "uuid",
  "code": 0,
  "msg": "success",
  "data": { ... }
}
```

---

## 9. 主题映射函数

### 9.1 statusColor(status)

根据状态字符串返回对应颜色：

| 输入 | 输出颜色 | 用途 |
|------|----------|------|
| online, ok, running, success, pass, normal | success (绿) | 正常状态 |
| offline, stopped, fail, failed, error | danger (红) | 异常状态 |
| maintenance, warn, warning, late, early | warning (橙) | 警告状态 |
| manual, info | info (蓝) | 信息状态 |
| 其他 | textMuted (灰) | 默认 |

### 9.2 attendancePieColor(status)

考勤饼图专用颜色映射：

| 输入 | 输出颜色 |
|------|----------|
| normal, manual | `#16a34a` (绿) |
| late | `#f97316` (橙) |
| early | `#8b5cf6` (紫) |
| absent | `#ef4444` (红) |

### 9.3 formatAttendanceStatus(code)

考勤状态中文格式化：

| 输入 | 输出 |
|------|------|
| normal | 正常 |
| late | 迟到 |
| early | 早退 |
| absent | 缺勤 |
| manual | 补签 |

---

## 10. 构建与运行

### 10.1 构建命令

```bash
# 从项目根目录执行
rebuild_x64.bat
```

### 10.2 输出路径

```
out/build/debug/AttendanceAdmin.exe
```

### 10.3 运行环境要求

- Windows 10/11
- MSVC 2022 运行时
- Qt 6.10.2 (msvc2022_64)

---

## 11. 文件目录结构

```
AttendanceServer/
├── ui/                          # QML UI 文件
│   ├── Main.qml                 # 主窗口
│   ├── LoginPage.qml            # 登录页面
│   ├── theme/
│   │   └── Theme.qml            # 设计令牌
│   ├── pages/                   # 业务页面
│   │   ├── PageDashboard.qml
│   │   ├── PagePerson.qml
│   │   ├── PageDevice.qml
│   │   ├── PageConfigDeploy.qml
│   │   ├── PageAttendance.qml
│   │   ├── PageFace.qml
│   │   ├── PageUser.qml
│   │   ├── PageRbac.qml
│   │   └── PageLogViewer.qml
│   ├── components/              # 可复用组件
│   │   ├── Card.qml
│   │   ├── DataTable.qml
│   │   ├── SideBar.qml
│   │   ├── StatusBar.qml
│   │   ├── ToolBarRow.qml
│   │   ├── LabeledField.qml
│   │   ├── BadgeStatus.qml
│   │   ├── StatCard.qml
│   │   ├── PermissionButton.qml
│   │   ├── ConfirmDialog.qml
│   │   ├── BusyOverlay.qml
│   │   ├── PageHost.qml
│   │   ├── PageAccessPlaceholder.qml
│   │   ├── PermissionDeniedDialog.qml
│   │   ├── WindowCaptionButtons.qml
│   │   ├── LoginServerSettingsDialog.qml
│   │   ├── LogPanel.qml
│   │   ├── JsonViewer.qml
│   │   ├── JsonEditor.qml
│   │   ├── FaceSyncButton.qml
│   │   ├── RecentPunchList.qml
│   │   ├── AttendanceStatusPieChart.qml
│   │   ├── BarChart.qml
│   │   ├── LineChart.qml
│   │   ├── ChartContainer.qml
│   │   └── ChartLegend.qml
│   └── models/                  # QML 单例模型
│       ├── PermissionCatalog.qml
│       ├── ErrorCatalog.qml
│       ├── Presets.qml
│       ├── History.qml
│       ├── Logger.qml
│       ├── LogReader.qml
│       └── PushFeed.qml
├── src/                         # C++ 源码
│   ├── Network/
│   │   └── TcpConnectionManager/
│   ├── Person/
│   ├── Device/
│   ├── Attendance/
│   ├── Config/
│   ├── Face/
│   ├── User/
│   ├── Rbac/
│   ├── Event/
│   └── Log/
├── CMakeLists.txt
└── rebuild_x64.bat
```

---

## 12. 设计规范总结

### 12.1 视觉风格
- **扁平化设计**: 无渐变、无立体效果
- **卡片式布局**: 白色卡片 + 细边框 + 微阴影
- **企业蓝色系**: 专业、稳重的配色方案
- **充足留白**: 使用 16px 页面边距

### 12.2 交互规范
- **悬停反馈**: 背景色变化 (`#CBD5E1`)
- **选中反馈**: 浅蓝色背景 (`#C7DFFF`)
- **加载状态**: 半透明遮罩 + 旋转动画
- **操作确认**: 危险操作弹出确认对话框

### 12.3 文字规范
- **主文字**: 13px, `#1F2937`
- **次要文字**: 12px, `#3D4551`
- **辅助文字**: 11px, `#6B7280`
- **错误文字**: 12px, `#C62828`

### 12.4 组件复用模式
1. **Card** 作为所有内容容器
2. **ToolBarRow** 作为所有页面标题栏
3. **DataTable** 作为所有数据列表
4. **PermissionButton** 作为所有操作按钮
5. **LabeledField** 作为所有表单字段

---

*文档生成时间: 2026-06-15*
*基于项目源码分析*
