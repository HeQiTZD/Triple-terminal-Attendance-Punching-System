# 打卡端（Attendance Check Client）开发文档

## 一、项目概述

### 1.1 项目定位
本项目为局域网智能考勤系统的打卡端，部署在考勤现场，负责人脸采集、识别、本地打卡、断网缓存、TCP 上传数据。

### 1.2 核心架构
- **架构模式**：C/S 局域网架构中的客户端
- **核心职责**：人脸识别打卡执行端
- **技术栈**：Qt 5.15/6.2+、C++11+、CMake、SQLite3、dlib/ArcFace 人脸识别库
- **Qt 依赖模块**：Core、Gui、Widgets、Network、Sql、Multimedia

## 二、核心功能

1. **人脸识别**：使用 dlib/ArcFace 库进行人脸检测和识别
2. **本地打卡**：识别成功后生成打卡记录并存储到本地数据库
3. **断网缓存**：网络断开时，打卡记录存储在本地，待网络恢复后自动上传
4. **TCP 上传**：主动连接管理端，定时或实时上传打卡记录
5. **设备状态监控**：显示网络连接状态、设备运行状态

## 三、模块结构

- **FaceRecognition**：人脸识别核心模块
- **CameraCapture**：摄像头采集模块
- **LocalStorage**：本地数据库操作模块
- **NetworkClient**：TCP 客户端模块
- **CacheManager**：断网缓存管理模块
- **UI**：用户界面模块

## 四、流程设计

1. 启动打卡端，初始化摄像头和人脸识别模型
2. 连接管理端，获取最新人员数据
3. 实时采集人脸，进行识别
4. 识别成功后生成打卡记录，存储到本地数据库
5. 尝试上传打卡记录到管理端
6. 网络断开时，将记录标记为待上传，网络恢复后自动重试

## 五、数据库设计

### 5.1 人员表（Person）
- id：主键
- name：姓名
- employee_id：工号
- face_feature：人脸特征数据
- last_updated：最后更新时间

### 5.2 打卡记录表（AttendanceRecord）
- id：主键
- person_id：人员ID
- check_time：打卡时间
- status：打卡状态（正常/异常）
- uploaded：是否已上传
- upload_time：上传时间

## 六、网络通信设计

### 6.1 TCP 连接管理
- 使用 QTcpSocket 主动连接管理端，保持长连接
- 连接状态检测：定时心跳机制，确保连接稳定性

### 6.2 数据传输协议
- **数据格式**：JSON 格式，便于解析和扩展
- **消息类型**：
  - 人员数据同步
  - 打卡记录上传
  - 设备状态上报
  - 配置下发

### 6.3 断网重连机制
- 网络断开后，定时尝试重连
- 网络恢复后自动同步数据

## 七、技术实现要点

### 7.1 人脸识别实现
- 使用 dlib 或 ArcFace 库进行人脸检测和特征提取
- 建立人脸特征库，支持快速比对
- 优化识别算法，提高识别速度和准确率

### 7.2 断网缓存机制
- 本地数据库存储待上传记录
- 网络恢复后自动批量上传
- 确保数据一致性，避免重复上传

### 7.3 性能优化
- 优化摄像头采集和图像处理速度
- 合理设计数据库索引，提高查询效率
- 优化网络传输，减少数据传输量

## 八、部署方案

- **安装位置**：考勤现场的电脑或专用设备
- **配置要求**：
  - 配置管理端 IP 和端口
  - 确保局域网内可以访问管理端
  - 安装必要的依赖库（dlib/ArcFace）

## 九、测试方案

- **功能测试**：测试人脸识别、打卡记录、数据同步等核心功能
- **网络测试**：测试断网情况下的离线打卡和恢复后的同步
- **性能测试**：测试人脸识别速度和系统响应时间
- **稳定性测试**：长时间运行测试，确保系统稳定

## 十、项目结构

```
AttendanceCheckClient/
├── CMakeLists.txt
├── src/
│   ├── FaceRecognition/     # 人脸识别模块
│   ├── CameraCapture/       # 摄像头采集模块
│   ├── LocalStorage/        # 本地存储模块
│   ├── NetworkClient/       # 网络客户端模块
│   ├── CacheManager/        # 缓存管理模块
│   └── UI/                  # 用户界面模块
└── resources/               # 资源文件
```
