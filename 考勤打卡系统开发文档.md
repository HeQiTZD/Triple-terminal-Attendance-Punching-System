# 考勤打卡系统开发文档

## 项目概述

### 项目简介
本项目是一个基于人脸识别技术的智能考勤打卡系统，通过摄像头采集人脸图像，使用ArcFace算法进行人脸识别，并与数据库中的员工信息进行比对，实现自动考勤打卡功能。

### 技术栈
- **开发框架**: Qt 6.x
- **开发语言**: C++17 
- **人脸识别**: ArcFace (百度开源)
- **图像处理**: dlib 19.24+
- **数据库**: SQLite 3
- **构建工具**: CMake 3.20+
- **编译器**: MSVC 2022 / MinGW 11+

### 项目目标
- 实现实时摄像头人脸采集
- 实现高精度人脸识别与比对
- 实现员工信息管理
- 实现考勤记录的增删改查
- 提供友好的图形用户界面

---

## 系统架构设计

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         用户界面层 (UI)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  主窗口     │  │  员工管理   │  │  考勤记录   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                       业务逻辑层 (Service)                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  人脸服务   │  │  员工服务   │  │  考勤服务   │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                       数据访问层 (DAO)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  员工DAO    │  │  考勤DAO    │  │  人脸特征DAO│         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                         基础设施层                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  摄像头管理  │  │  图像处理   │  │  数据库连接  │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
```

### 模块划分

#### 1. 摄像头模块 (CameraModule)
- **职责**: 管理摄像头连接、帧捕获
- **核心类**: `CameraManager`
- **功能**:
  - 枚举可用摄像头设备
  - 打开/关闭摄像头
  - 实时捕获视频帧
  - 帧数据回调

#### 2. 图像处理模块 (ImageProcessingModule)
- **职责**: 人脸检测、图像预处理
- **核心类**: `FaceDetector`, `ImagePreprocessor`
- **功能**:
  - 人脸检测与定位
  - 人脸关键点检测
  - 图像归一化
  - 人脸对齐

#### 3. 人脸识别模块 (FaceRecognitionModule)
- **职责**: 人脸特征提取与比对
- **核心类**: `FaceRecognizer`
- **功能**:
  - 人脸特征提取 (128维特征向量)
  - 特征相似度计算 (余弦相似度)
  - 人脸比对阈值判断

#### 4. 数据库模块 (DatabaseModule)
- **职责**: 数据持久化
- **核心类**: `DatabaseManager`, `EmployeeDAO`, `AttendanceDAO`, `FaceFeatureDAO`
- **功能**:
  - 数据库连接管理
  - 员工信息CRUD
  - 考勤记录CRUD
  - 人脸特征存储与检索

#### 5. 业务逻辑模块 (BusinessLogicModule)
- **职责**: 业务流程编排
- **核心类**: `AttendanceService`, `EmployeeService`, `FaceService`
- **功能**:
  - 考勤打卡流程
  - 员工注册流程
  - 人脸注册流程
  - 考勤统计

#### 6. 用户界面模块 (UIModule)
- **职责**: 图形交互
- **核心类**: `MainWindow`, `EmployeeManagementDialog`, `AttendanceRecordDialog`
- **功能**:
  - 实时摄像头预览
  - 打卡结果显示
  - 员工信息管理界面
  - 考勤记录查询界面

---

## 数据库设计

### 数据库表结构

#### 1. 员工表 (employees)
```sql
CREATE TABLE employees (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_no TEXT NOT NULL UNIQUE,
    name TEXT NOT NULL,
    department TEXT,
    position TEXT,
    phone TEXT,
    email TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### 2. 人脸特征表 (face_features)
```sql
CREATE TABLE face_features (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_id INTEGER NOT NULL,
    feature_data BLOB NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES employees(id) ON DELETE CASCADE
);
```

#### 3. 考勤记录表 (attendance_records)
```sql
CREATE TABLE attendance_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    employee_id INTEGER NOT NULL,
    check_in_time DATETIME,
    check_out_time DATETIME,
    check_in_image BLOB,
    check_out_image BLOB,
    status TEXT DEFAULT 'normal',
    remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (employee_id) REFERENCES employees(id) ON DELETE CASCADE
);
```

### 索引设计
```sql
CREATE INDEX idx_employees_no ON employees(employee_no);
CREATE INDEX idx_face_features_employee ON face_features(employee_id);
CREATE INDEX idx_attendance_employee ON attendance_records(employee_id);
CREATE INDEX idx_attendance_date ON attendance_records(date(check_in_time));
```

---

## 核心类设计

### CameraManager 类
```cpp
class CameraManager : public QObject
{
    Q_OBJECT

public:
    explicit CameraManager(QObject* parent = nullptr);
    ~CameraManager();

    bool openCamera(int deviceIndex = 0);
    void closeCamera();
    bool isOpened() const;
    QList<QCameraDevice> availableCameras() const;

signals:
    void frameCaptured(const QImage& frame);
    void errorOccurred(const QString& error);

private:
    QCamera* m_camera;
    QMediaCaptureSession* m_captureSession;
    QVideoSink* m_videoSink;
};
```

### FaceDetector 类
```cpp
class FaceDetector
{
public:
    FaceDetector();
    ~FaceDetector();

    bool initialize();
    std::vector<dlib::rectangle> detectFaces(const dlib::array2d<dlib::rgb_pixel>& image);
    dlib::array2d<dlib::rgb_pixel> alignFace(const dlib::array2d<dlib::rgb_pixel>& image, const dlib::rectangle& faceRect);

private:
    dlib::frontal_face_detector m_detector;
    dlib::shape_predictor m_shapePredictor;
    bool m_initialized;
};
```

### FaceRecognizer 类
```cpp
class FaceRecognizer
{
public:
    FaceRecognizer();
    ~FaceRecognizer();

    bool initialize(const QString& modelPath);
    std::vector<float> extractFeature(const dlib::array2d<dlib::rgb_pixel>& faceImage);
    float calculateSimilarity(const std::vector<float>& feature1,
                               const std::vector<float>& feature2);
    bool compareFaces(const std::vector<float>& feature1,
                      const std::vector<float>& feature2,
                      float threshold = 0.6f);

private:
    arcface::FaceRecognitionModel* m_model;
    bool m_initialized;
};
```

### DatabaseManager 类
```cpp
class DatabaseManager
{
public:
    static DatabaseManager& instance();

    bool openDatabase(const QString& dbPath);
    void closeDatabase();
    bool isOpen() const;
    QSqlDatabase database() const;

private:
    DatabaseManager();
    ~DatabaseManager();
    QSqlDatabase m_database;
    bool m_initialized;
};
```

### AttendanceService 类
```cpp
class AttendanceService : public QObject
{
    Q_OBJECT

public:
    explicit AttendanceService(QObject* parent = nullptr);

    bool checkIn(int employeeId, const QImage& faceImage);
    bool checkOut(int employeeId, const QImage& faceImage);
    QList<AttendanceRecord> getTodayRecords();
    QList<AttendanceRecord> getRecordsByDateRange(const QDate& start, const QDate& end);

signals:
    void checkInSuccess(const AttendanceRecord& record);
    void checkOutSuccess(const AttendanceRecord& record);
    void recognitionFailed(const QString& reason);
};
```

---

## 核心流程设计

### 1. 员工注册流程
```
用户输入员工信息
    ↓
验证员工号唯一性
    ↓
保存员工信息到数据库
    ↓
提示用户采集人脸
    ↓
摄像头采集人脸图像
    ↓
人脸检测与对齐
    ↓
提取人脸特征
    ↓
保存人脸特征到数据库
    ↓
注册完成
```

### 2. 考勤打卡流程
```
启动摄像头预览
    ↓
实时人脸检测
    ↓
检测到人脸
    ↓
人脸对齐
    ↓
提取人脸特征
    ↓
从数据库加载所有人脸特征
    ↓
逐一比对计算相似度
    ↓
找到匹配员工 (相似度 > 阈值)
    ↓
判断是签到还是签退
    ↓
保存考勤记录
    ↓
显示打卡结果
```

### 3. 人脸识别比对流程
```
输入: 待识别人脸特征 F
    ↓
从数据库获取所有已注册特征集合 {F1, F2, ..., Fn}
    ↓
初始化 maxSimilarity = 0, matchedId = -1
    ↓
遍历每个特征 Fi:
    计算相似度 sim = cosine(F, Fi)
    如果 sim > maxSimilarity:
        maxSimilarity = sim
        matchedId = i
    ↓
如果 maxSimilarity > threshold:
    返回匹配的员工ID
否则:
    返回未识别
```

---

## 项目结构

```
attendance-system/
├── CMakeLists.txt
├── src/
│   ├── main.cpp
│   ├── ui/
│   │   ├── MainWindow.h
│   │   ├── MainWindow.cpp
│   │   ├── EmployeeManagementDialog.h
│   │   ├── EmployeeManagementDialog.cpp
│   │   ├── AttendanceRecordDialog.h
│   │   └── AttendanceRecordDialog.cpp
│   ├── core/
│   │   ├── CameraManager.h
│   │   ├── CameraManager.cpp
│   │   ├── FaceDetector.h
│   │   ├── FaceDetector.cpp
│   │   ├── FaceRecognizer.h
│   │   └── FaceRecognizer.cpp
│   ├── database/
│   │   ├── DatabaseManager.h
│   │   ├── DatabaseManager.cpp
│   │   ├── EmployeeDAO.h
│   │   ├── EmployeeDAO.cpp
│   │   ├── AttendanceDAO.h
│   │   ├── AttendanceDAO.cpp
│   │   ├── FaceFeatureDAO.h
│   │   └── FaceFeatureDAO.cpp
│   ├── service/
│   │   ├── AttendanceService.h
│   │   ├── AttendanceService.cpp
│   │   ├── EmployeeService.h
│   │   └── EmployeeService.cpp
│   └── model/
│       ├── Employee.h
│       ├── AttendanceRecord.h
│       └── FaceFeature.h
├── resources/
│   ├── models/
│   │   ├── shape_predictor_68_face_landmarks.dat
│   │   └── arcface_model.onnx
│   ├── icons/
│   └── qss/
├── tests/
│   ├── CMakeLists.txt
│   ├── test_face_detector.cpp
│   ├── test_face_recognizer.cpp
│   └── test_database.cpp
└── docs/
    └── 开发文档.md
```

---

## CMakeLists.txt 配置

```cmake
cmake_minimum_required(VERSION 3.20)
project(AttendanceSystem VERSION 1.0.0 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTORCC ON)
set(CMAKE_AUTOUIC ON)

find_package(Qt6 REQUIRED COMPONENTS Core Gui Widgets Multimedia MultimediaWidgets Sql)

# 配置dlib
set(DLIB_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/third_party/dlib/include)
set(DLIB_LIBRARY ${CMAKE_CURRENT_SOURCE_DIR}/third_party/dlib/build/dlib.lib)

set(PROJECT_SOURCES
    src/main.cpp
    src/ui/MainWindow.cpp
    src/ui/EmployeeManagementDialog.cpp
    src/ui/AttendanceRecordDialog.cpp
    src/core/CameraManager.cpp
    src/core/FaceDetector.cpp
    src/core/FaceRecognizer.cpp
    src/database/DatabaseManager.cpp
    src/database/EmployeeDAO.cpp
    src/database/AttendanceDAO.cpp
    src/database/FaceFeatureDAO.cpp
    src/service/AttendanceService.cpp
    src/service/EmployeeService.cpp
)

set(PROJECT_HEADERS
    src/ui/MainWindow.h
    src/ui/EmployeeManagementDialog.h
    src/ui/AttendanceRecordDialog.h
    src/core/CameraManager.h
    src/core/FaceDetector.h
    src/core/FaceRecognizer.h
    src/database/DatabaseManager.h
    src/database/EmployeeDAO.h
    src/database/AttendanceDAO.h
    src/database/FaceFeatureDAO.h
    src/service/AttendanceService.h
    src/service/EmployeeService.h
    src/model/Employee.h
    src/model/AttendanceRecord.h
    src/model/FaceFeature.h
)

add_executable(${PROJECT_NAME}
    ${PROJECT_SOURCES}
    ${PROJECT_HEADERS}
)

target_link_libraries(${PROJECT_NAME} PRIVATE
    Qt6::Core
    Qt6::Gui
    Qt6::Widgets
    Qt6::Multimedia
    Qt6::MultimediaWidgets
    Qt6::Sql
    ${DLIB_LIBRARY}
)

target_include_directories(${PROJECT_NAME} PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/src
    ${DLIB_INCLUDE_DIR}
)

if(WIN32)
    set_target_properties(${PROJECT_NAME} PROPERTIES
        WIN32_EXECUTABLE TRUE
    )
endif()
```

---

## 环境配置

### 开发环境要求
- Windows 10/11
- Visual Studio 2022 或 MinGW 11+
- Qt 6.5+
- dlib 19.24+
- CMake 3.20+

### 依赖库安装

#### 1. Qt 安装
```bash
# 使用在线安装器
# 下载地址: https://www.qt.io/download
# 选择组件: Qt 6.5.x, MSVC 2019/2022, MinGW
```

#### 2. dlib 安装
```bash
# 从GitHub克隆并编译
# 克隆仓库
git clone https://github.com/davisking/dlib.git

# 编译dlib
cd dlib
mkdir build
cd build
cmake ..
cmake --build . --config Release

# 将编译好的库文件和头文件复制到项目中
# 头文件: dlib/include/
# 库文件: dlib/build/dlib.lib
```

#### 3. ArcFace SDK 配置
```bash
# 下载 ArcFace SDK
# 下载地址: https://ai.baidu.com/tech/face
# 将模型文件放置在 resources/models/ 目录
```

---

## 关键技术实现

### 1. 摄像头实时捕获
使用 Qt Multimedia 模块实现摄像头捕获，通过 QVideoSink 获取每一帧数据。

### 2. 人脸检测
使用 dlib 的 frontal_face_detector 进行人脸检测，使用 shape_predictor 进行关键点检测。

### 3. 人脸识别
集成 ArcFace SDK 提取 128 维人脸特征向量，使用余弦相似度进行比对。

### 4. 特征向量存储
将 float 数组序列化后以 BLOB 格式存储在 SQLite 数据库中。

### 5. 相似度计算
```cpp
float FaceRecognizer::calculateSimilarity(const std::vector<float>& feature1,
                                           const std::vector<float>& feature2)
{
    float dot = 0.0f;
    float norm1 = 0.0f;
    float norm2 = 0.0f;

    for (size_t i = 0; i < feature1.size(); ++i) {
        dot += feature1[i] * feature2[i];
        norm1 += feature1[i] * feature1[i];
        norm2 += feature2[i] * feature2[i];
    }

    return dot / (std::sqrt(norm1) * std::sqrt(norm2));
}
```

---

## 测试计划

### 单元测试
- 人脸检测准确率测试
- 特征提取一致性测试
- 相似度计算准确性测试
- 数据库 CRUD 操作测试

### 集成测试
- 员工注册完整流程测试
- 考勤打卡完整流程测试
- 人脸识别比对测试 (不同光照、角度、表情)

### 性能测试
- 单帧人脸检测耗时 < 50ms
- 单人人脸特征提取耗时 < 100ms
- 100人库比对耗时 < 500ms

---

## 部署说明

### 打包依赖
```bash
# 使用 windeployqt
windeployqt AttendanceSystem.exe

# 手动复制依赖
# - dlib 相关文件
# - ArcFace SDK DLLs
# - 模型文件
```

### 目录结构
```
AttendanceSystem/
├── AttendanceSystem.exe
├── Qt6Core.dll
├── Qt6Gui.dll
├── Qt6Widgets.dll
├── Qt6Multimedia.dll
├── Qt6Sql.dll
├── dlib.dll
├── arcface.dll
├── data/
│   ├── attendance.db
│   └── models/
│       ├── shape_predictor_68_face_landmarks.dat
│       └── arcface_model.onnx
└── platforms/
    └── qwindows.dll
```

---

## 未来优化方向

1. **算法优化**
   - 集成更先进的人脸识别模型 (如 ArcFace++, InsightFace)
   - 添加活体检测功能，防止照片攻击
   - 优化人脸对齐算法

2. **功能扩展**
   - 添加多人同时识别
   - 支持考勤报表导出 (Excel/PDF)
   - 添加网络同步功能

3. **性能优化**
   - 使用 GPU 加速推理
   - 实现特征向量索引加速检索
   - 优化内存使用

4. **用户体验**
   - 添加语音提示
   - 优化 UI/UX 设计
   - 添加主题切换功能

---

## 项目亮点

1. **完整的分层架构**: 采用经典的三层架构，代码结构清晰，易于维护和扩展
2. **高性能人脸识别**: 集成 ArcFace 算法，识别准确率高，速度快
3. **跨平台支持**: 基于 Qt 框架，可轻松移植到 Linux/macOS
4. **完整的业务流程**: 涵盖员工管理、人脸注册、考勤打卡、记录查询等完整功能
5. **现代化技术栈**: 使用 C++17、Qt 6、dlib 等主流技术

---

## 参考资料

- Qt 官方文档: https://doc.qt.io/
- dlib 官方文档: http://dlib.net/documentation.html
- ArcFace SDK 文档: https://ai.baidu.com/ai-doc/FACE/
- SQLite 官方文档: https://www.sqlite.org/docs.html